/**
 * GENERATED by apx-testgen from pages/p00023-invitations-sent.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class InvitationsSentPage {
  static readonly alias = 'invitations-sent';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, InvitationsSentPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** displayOnly */
  get otherInviteesMsg(): ApexItem {
    return new ApexItem(this.page, 'P23_OTHER_INVITEES_MSG');
  }

  /** "Invite More" */
  async clickInviteMore(): Promise<void> {
    await buttonByLabel(this.page, 'Invite More').click();
  }

  /** "View Emails" */
  async clickViewEmails(): Promise<void> {
    await buttonByLabel(this.page, 'View Emails').click();
  }

  /** "View Invitations" */
  async clickViewInvites(): Promise<void> {
    await buttonByLabel(this.page, 'View Invitations').click();
  }

  /** "Manage Poll" */
  async clickViewPoll(): Promise<void> {
    await buttonByLabel(this.page, 'Manage Poll').click();
  }
}
