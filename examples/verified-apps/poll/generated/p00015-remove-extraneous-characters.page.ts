/**
 * GENERATED by apx-testgen from pages/p00015-remove-extraneous-characters.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class RemoveExtraneousCharactersPage {
  static readonly alias = 'remove-extraneous-characters';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, RemoveExtraneousCharactersPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textField, "ASCII Character to Scrub", required */
  get charToScrub(): ApexItem {
    return new ApexItem(this.page, 'P15_CHAR_TO_SCRUB');
  }

  /** displayOnly, "Community References Updated" */
  get commRefCnt(): ApexItem {
    return new ApexItem(this.page, 'P15_COMM_REF_CNT');
  }

  /** displayOnly, "Invitations Updates" */
  get inviteCnt(): ApexItem {
    return new ApexItem(this.page, 'P15_INVITE_CNT');
  }

  /** displayOnly, "Matching Respondents Removed" */
  get matchingRespCnt(): ApexItem {
    return new ApexItem(this.page, 'P15_MATCHING_RESP_CNT');
  }

  /** displayOnly, "Non-matching Respondents Updated" */
  get nonMatchRespCnt(): ApexItem {
    return new ApexItem(this.page, 'P15_NON_MATCH_RESP_CNT');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "OK" */
  async clickOk(): Promise<void> {
    await buttonByLabel(this.page, 'OK').click();
  }

  /** "Go" */
  async clickP15Go(): Promise<void> {
    await buttonByLabel(this.page, 'Go').click();
  }

  /** "Scrub Data" */
  async clickScrubData(): Promise<void> {
    await buttonByLabel(this.page, 'Scrub Data').click();
  }
}
