/**
 * GENERATED by apx-testgen from pages/p00090-email-results.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class EmailResultsPage {
  static readonly alias = 'email-results';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, EmailResultsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Community" */
  get communityId(): ApexItem {
    return new ApexItem(this.page, 'P90_COMMUNITY_ID');
  }

  /** displayOnly, "Email From" */
  get emailFrom(): ApexItem {
    return new ApexItem(this.page, 'P90_EMAIL_FROM');
  }

  /** textarea, "Addresses to email results to" */
  get emailTo(): ApexItem {
    return new ApexItem(this.page, 'P90_EMAIL_TO');
  }

  /** textarea, "Introduction Text" */
  get introText(): ApexItem {
    return new ApexItem(this.page, 'P90_INTRO_TEXT');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Email Results" */
  async clickEmailResults(): Promise<void> {
    await buttonByLabel(this.page, 'Email Results').click();
  }
}
