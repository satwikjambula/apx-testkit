/**
 * GENERATED by apx-testgen from pages/p00050-poll-form.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00050-poll-form.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: bottom-bar, breadcrumb, error, errors-in-submission, hidden-items, hide-nobody, new, render-poll, warnings
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): bottom-bar (staticContent), breadcrumb (breadcrumb), error (plSqlDynamicContent), errors-in-submission (plSqlDynamicContent), hidden-items (staticContent), hide-nobody (staticContent), new (staticContent), render-poll (plSqlDynamicContent), warnings (plSqlDynamicContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { PollFormPage } from './p00050-poll-form.page.js';

test.describe('page 50: &POLL_NAME.', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new PollFormPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new PollFormPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&POLL_NAME.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (6 items, 6 hidden)', async ({ page }) => {
    const po = new PollFormPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P50_CAN_TAKE_YN', 'P50_HAS_ERROR', 'P50_NBR_QUESTIONS', 'P50_RESULT_ID', 'P50_WARNING_MESSAGE', 'PID']);
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new PollFormPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Submit']);
  });
});
