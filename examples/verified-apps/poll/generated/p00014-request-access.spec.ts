/**
 * GENERATED by apx-testgen from pages/p00014-request-access.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00014-request-access.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: hide-nobody, request-access
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): hide-nobody (staticContent), request-access (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { RequestAccessPage } from './p00014-request-access.page.js';

test.describe('page 14: &APPLICATION_TITLE. - Request Access', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new RequestAccessPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new RequestAccessPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&APPLICATION_TITLE. - Request Access'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (5 items, 2 hidden)', async ({ page }) => {
    const po = new RequestAccessPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P14_EMAIL_ADDRESS', 'P14_ID', 'P14_JUSTIFICATION', 'P14_STATUS', 'P14_USERNAME']);
  });

  test('apex.item round-trip on P14_EMAIL_ADDRESS', async ({ page }) => {
    const po = new RequestAccessPage(page);
    await po.goto();
    await po.emailAddress.setValue('apx-testgen');
    expect(await po.emailAddress.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new RequestAccessPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Request Access']);
  });
});
