/**
 * GENERATED by apx-testgen from pages/p00180-for-question-image-display.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00180-for-question-image-display.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: region
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): region (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ForQuestionImageDisplayPage } from './p00180-for-question-image-display.page.js';

test.describe('page 180: for question image display', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ForQuestionImageDisplayPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ForQuestionImageDisplayPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('for question image display'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 1 hidden)', async ({ page }) => {
    const po = new ForQuestionImageDisplayPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P180_ID', 'P180_IMAGE_BLOB']);
  });
});
