/**
 * GENERATED by apx-testgen from pages/p00036-manual-invitations-recorded.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ManualInvitationsRecordedPage {
  static readonly alias = 'manual-invitations-recorded';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ManualInvitationsRecordedPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** displayOnly */
  get otherInviteesMsg(): ApexItem {
    return new ApexItem(this.page, 'P36_OTHER_INVITEES_MSG');
  }

  /** "Invite More" */
  async clickInviteMore(): Promise<void> {
    await buttonByLabel(this.page, 'Invite More').click();
  }

  /** "View Invitations" */
  async clickViewLinks(): Promise<void> {
    await buttonByLabel(this.page, 'View Invitations').click();
  }

  /** "Manage Poll" */
  async clickViewPoll(): Promise<void> {
    await buttonByLabel(this.page, 'Manage Poll').click();
  }
}
