/**
 * GENERATED by apx-testgen from pages/p00068-poll-name.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class PollNamePage {
  static readonly alias = 'poll-name';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, PollNamePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get anonymousYn(): ApexItem {
    return new ApexItem(this.page, 'P68_ANONYMOUS_YN');
  }

  /** hidden */
  get nextQid(): ApexItem {
    return new ApexItem(this.page, 'P68_NEXT_QID');
  }

  /** hidden */
  get prevQid(): ApexItem {
    return new ApexItem(this.page, 'P68_PREV_QID');
  }

  /** hidden */
  get questionId(): ApexItem {
    return new ApexItem(this.page, 'P68_QUESTION_ID');
  }

  /** hidden */
  get questionNbr(): ApexItem {
    return new ApexItem(this.page, 'P68_QUESTION_NBR');
  }

  /** "Next" */
  async clickNext(): Promise<void> {
    await buttonByLabel(this.page, 'Next').click();
  }

  /** "Next" */
  async clickNextDisabled(): Promise<void> {
    await buttonByLabel(this.page, 'Next').click();
  }

  /** "Previous" */
  async clickPrevious(): Promise<void> {
    await buttonByLabel(this.page, 'Previous').click();
  }

  /** "Previous" */
  async clickPreviousDisabled(): Promise<void> {
    await buttonByLabel(this.page, 'Previous').click();
  }

  /** "Refresh Results" */
  async clickRefreshResults(): Promise<void> {
    await buttonByLabel(this.page, 'Refresh Results').click();
  }
}
