/**
 * GENERATED by apx-testgen from pages/p00071-community.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class CommunityPage {
  static readonly alias = 'community';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, CommunityPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textField, "Copy Respondents From" */
  get communityFrom(): ApexItem {
    return new ApexItem(this.page, 'P71_COMMUNITY_FROM');
  }

  /** displayOnly, "Community Name" */
  get communityName(): ApexItem {
    return new ApexItem(this.page, 'P71_COMMUNITY_NAME');
  }

  /** textField, "Copy Respondents To" */
  get communityTo(): ApexItem {
    return new ApexItem(this.page, 'P71_COMMUNITY_TO');
  }

  /** hidden */
  get invalidCount(): ApexItem {
    return new ApexItem(this.page, 'P71_INVALID_COUNT');
  }

  /** hidden */
  get validCount(): ApexItem {
    return new ApexItem(this.page, 'P71_VALID_COUNT');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Add Community / Members" */
  async clickFinish(): Promise<void> {
    await buttonByLabel(this.page, 'Add Community / Members').click();
  }

  /** "Previous" */
  async clickPrevious(): Promise<void> {
    await buttonByLabel(this.page, 'Previous').click();
  }
}
