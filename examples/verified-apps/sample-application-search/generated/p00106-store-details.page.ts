/**
 * GENERATED by apx-testgen from pages/p00106-store-details.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class StoreDetailsPage {
  static readonly alias = 'store-details';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, StoreDetailsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get latitude(): ApexItem {
    return new ApexItem(this.page, 'P106_LATITUDE');
  }

  /** hidden */
  get longitude(): ApexItem {
    return new ApexItem(this.page, 'P106_LONGITUDE');
  }

  /** hidden */
  get physicalAddress(): ApexItem {
    return new ApexItem(this.page, 'P106_PHYSICAL_ADDRESS');
  }

  /** hidden */
  get storeId(): ApexItem {
    return new ApexItem(this.page, 'P106_STORE_ID');
  }

  /** hidden */
  get storeName(): ApexItem {
    return new ApexItem(this.page, 'P106_STORE_NAME');
  }

  /** hidden */
  get webAddress(): ApexItem {
    return new ApexItem(this.page, 'P106_WEB_ADDRESS');
  }
}
