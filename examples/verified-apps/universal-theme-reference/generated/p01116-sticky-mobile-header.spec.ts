/**
 * GENERATED by apx-testgen from pages/p01116-sticky-mobile-header.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01116-sticky-mobile-header.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: header-buttons, page-header, region-a, region-b, region-c
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): header-buttons (staticContent), page-header (staticContent), region-a (staticContent), region-b (staticContent), region-c (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { StickyMobileHeaderPage } from './p01116-sticky-mobile-header.page.js';

test.describe('page 1116: Sticky Mobile Header', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new StickyMobileHeaderPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new StickyMobileHeaderPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Sticky Mobile Header'));
  });

  test('every labeled .apx button exists (3 buttons)', async ({ page }) => {
    const po = new StickyMobileHeaderPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Actions', 'Back', 'Edit']);
  });
});
