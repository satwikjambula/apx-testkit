/**
 * GENERATED by apx-testgen from pages/p00407-navigation.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00407-navigation.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, mega-menu, menu-bar, navigation-bar, navigation-menus, overview, side-tree-navigation, tabs
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), mega-menu (staticContent), menu-bar (staticContent), navigation-bar (staticContent), navigation-menus (staticContent), overview (staticContent), side-tree-navigation (staticContent), tabs (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { NavigationPage } from './p00407-navigation.page.js';

test.describe('page 407: Navigation', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new NavigationPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new NavigationPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Navigation - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (4 buttons)', async ({ page }) => {
    const po = new NavigationPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['View', 'View', 'View', 'View']);
  });
});
