/**
 * GENERATED by apx-testgen from pages/p00425-jquery-mobile-components.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00425-jquery-mobile-components.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, column-toggle-examples, column-toggle-report, jquery-mobile-is-no-longer-supported, list-view, list-view-examples, mobile-ui-patterns, mobile-ui-patterns-2, overview, reflow-report, reflow-report-examples, responsive-design-and-mobile-ui-patterns
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), column-toggle-examples (staticContent), column-toggle-report (staticContent), jquery-mobile-is-no-longer-supported (staticContent), list-view (staticContent), list-view-examples (staticContent), mobile-ui-patterns (staticContent), mobile-ui-patterns-2 (list), overview (staticContent), reflow-report (staticContent), reflow-report-examples (staticContent), responsive-design-and-mobile-ui-patterns (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { JqueryMobileComponentsPage } from './p00425-jquery-mobile-components.page.js';

test.describe('page 425: jQuery Mobile Components', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new JqueryMobileComponentsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new JqueryMobileComponentsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('jQuery Mobile Components'));
  });

  test('every labeled .apx button exists (6 buttons)', async ({ page }) => {
    const po = new JqueryMobileComponentsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Example 1', 'Example 2', 'Example 1', 'Example 2', 'Example 1', 'Example 2']);
  });
});
