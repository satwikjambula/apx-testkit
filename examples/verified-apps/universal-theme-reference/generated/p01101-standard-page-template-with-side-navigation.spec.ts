/**
 * GENERATED by apx-testgen from pages/p01101-standard-page-template-with-side-navigation.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01101-standard-page-template-with-side-navigation.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, region-a, region-b, region-c
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), region-a (staticContent), region-b (staticContent), region-c (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { StandardPageTemplateWithSideNavigationPage } from './p01101-standard-page-template-with-side-navigation.page.js';

test.describe('page 1101: Standard Page Template with Side Navigation', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new StandardPageTemplateWithSideNavigationPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new StandardPageTemplateWithSideNavigationPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Standard Page Template with Side Navigation - &APP_TITLE.'));
  });
});
