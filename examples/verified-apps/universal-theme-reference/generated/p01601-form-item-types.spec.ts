/**
 * GENERATED by apx-testgen from pages/p01601-form-item-types.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01601-form-item-types.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, overview, region-display-selector, region-with-items, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), region-with-items (staticContent), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { FormItemTypesPage } from './p01601-form-item-types.page.js';

test.describe('page 1601: Form Item Types', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new FormItemTypesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new FormItemTypesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Form Item Types'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (30 items, 1 hidden)', async ({ page }) => {
    const po = new FormItemTypesPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1601_CHECKBOX', 'P1601_CHECKBOX_GROUP', 'P1601_COLOR_PICKER', 'P1601_COMBOBOX', 'P1601_COMBO_BOX_MANUAL_ENTRIES', 'P1601_DATE_PICKER', 'P1601_DATE_PICKER_INLINE', 'P1601_DATE_PICKER_NATIVE', 'P1601_DISPLAY_MAP', 'P1601_DISPLAY_ONLY', 'P1601_FILE_BROWSE', 'P1601_IMAGE_UPLOAD', 'P1601_LIST_MANAGER', 'P1601_MARKDOWN_EDITOR', 'P1601_NUMBER_FIELD', 'P1601_PASSWORD', 'P1601_PERCENT_GRAPH', 'P1601_POPUP_LOV', 'P1601_QR_CODE', 'P1601_RADIO_GROUP', 'P1601_RADIO_GROUP_AS_BUTTONS', 'P1601_SELECT_LIST', 'P1601_SELECT_MANY', 'P1601_SELECT_ONE', 'P1601_SHUTTLE', 'P1601_STAR_RATING', 'P1601_SWITCH', 'P1601_TEXTAREA', 'P1601_TEXT_FIELD', 'P1601_TEXT_FIELD_WITH_AUTOCOMPLETE']);
  });

  test('apex.item round-trip on P1601_TEXTAREA', async ({ page }) => {
    const po = new FormItemTypesPage(page);
    await po.goto();
    await po.textarea.setValue('apx-testgen');
    expect(await po.textarea.getValue()).toBe('apx-testgen');
  });
});
