/**
 * GENERATED by apx-testgen from pages/p01500-buttons.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01500-buttons.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumbs, demo, demo-2, demo-3, demo-4, hot, icon-only, instructions, instructions-2, instructions-3, instructions-4, overview, region-display-selector, template-options, template-options-2, template-options-3, template-options-4, text-only, text-with-icon
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumbs (breadcrumb), demo (staticContent), demo-2 (staticContent), demo-3 (staticContent), demo-4 (staticContent), hot (staticContent), icon-only (staticContent), instructions (plugin/componentInstructions), instructions-2 (plugin/componentInstructions), instructions-3 (plugin/componentInstructions), instructions-4 (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions), template-options-3 (plugin/previewTemplateOptions), template-options-4 (plugin/previewTemplateOptions), text-only (staticContent), text-with-icon (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ButtonsPage } from './p01500-buttons.page.js';

test.describe('page 1500: Buttons', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ButtonsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ButtonsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Buttons - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (4 buttons)', async ({ page }) => {
    const po = new ButtonsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Button', 'Button', 'Button', 'Button']);
  });
});
