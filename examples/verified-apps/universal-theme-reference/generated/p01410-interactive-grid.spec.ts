/**
 * GENERATED by apx-testgen from pages/p01410-interactive-grid.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01410-interactive-grid.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: basic-reporting, breadcrumb, demo, instructions, overview, region-display-selector, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Interactive Grid view-check emitted for 1 region(s) with a known runtime id (htmlDomId set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, ApexInteractiveGridRegion, normalizeTitle } from '@apx/testkit';
import { InteractiveGridPage } from './p01410-interactive-grid.page.js';

test.describe('page 1410: Interactive Grid', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new InteractiveGridPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new InteractiveGridPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Interactive Grid - &APP_TITLE.'));
  });

  test('every Interactive Grid region with a known runtime id resolves a current view (1 region)', async ({ page }) => {
    const po = new InteractiveGridPage(page);
    await po.goto();
    for (const id of ['Demo1']) {
      const ig = new ApexInteractiveGridRegion(page, id);
      expect(typeof await ig.getCurrentViewId()).toBe('string');
    }
  });
});
