/**
 * GENERATED by apx-testgen from pages/p01204-button-container-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01204-button-container-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: additional-example, breadcrumb, overview, region-display-selector, step, step-2, using-page-buttons, using-page-items
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): additional-example (staticContent), breadcrumb (breadcrumb), overview (staticContent), region-display-selector (regionDisplaySelector), step (staticContent), step-2 (staticContent), using-page-buttons (staticContent), using-page-items (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ButtonContainerRegionPage } from './p01204-button-container-region.page.js';

test.describe('page 1204: Region - Button Container', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ButtonContainerRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ButtonContainerRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Button Container - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 0 hidden)', async ({ page }) => {
    const po = new ButtonContainerRegionPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1204_RADIO_A', 'P1204_RADIO_B']);
  });

  test('every labeled .apx button exists (9 buttons)', async ({ page }) => {
    const po = new ButtonContainerRegionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Previous', 'Rewind', 'Play', 'Stop', 'Forward', 'Next', 'Button A', 'Button B', 'Button C']);
  });
});
