/**
 * GENERATED by apx-testgen from pages/p01100-pages.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01100-pages.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: both-side-column, breadcrumb, dialog, left-side-column, left-side-column-with-side-navigation, login-page-template, marquee-detail, master-detail-page-with-side-navigation, minimal-page-template, other, overview, region-display-selector, right-side-column, right-side-column-with-side-navigation, side-columns-page-with-side-navigation, standard, standard-dialog-page, standard-page-with-side-navigation, wizard-dialog-page
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): both-side-column (staticContent), breadcrumb (breadcrumb), dialog (staticContent), left-side-column (staticContent), left-side-column-with-side-navigation (staticContent), login-page-template (staticContent), marquee-detail (staticContent), master-detail-page-with-side-navigation (staticContent), minimal-page-template (staticContent), other (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), right-side-column (staticContent), right-side-column-with-side-navigation (staticContent), side-columns-page-with-side-navigation (staticContent), standard (staticContent), standard-dialog-page (staticContent), standard-page-with-side-navigation (staticContent), wizard-dialog-page (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { PagesPage } from './p01100-pages.page.js';

test.describe('page 1100: Pages', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new PagesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new PagesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Pages - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new PagesPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['View Sample', 'View Sample']);
  });
});
