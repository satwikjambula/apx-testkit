/**
 * GENERATED by apx-testgen from pages/p03100-card-templates.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03100-card-templates.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, cards-as-a-list-template, cards-as-a-report-template, cards-list, cards-report, column-aliases, demo, demo-2, instructions, instructions-2, list-custom-attributes, overview, region-display-selector, sample-sql-query, template-options, template-options-2
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), cards-as-a-list-template (staticContent), cards-as-a-report-template (staticContent), cards-list (list), cards-report (classicReport), column-aliases (staticContent), demo (staticContent), demo-2 (staticContent), instructions (plugin/componentInstructions), instructions-2 (plugin/componentInstructions), list-custom-attributes (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query (plugin/regionSourceCode), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { CardTemplatesPage } from './p03100-card-templates.page.js';

test.describe('page 3100: Card Templates', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new CardTemplatesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new CardTemplatesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Card Templates'));
  });
});
