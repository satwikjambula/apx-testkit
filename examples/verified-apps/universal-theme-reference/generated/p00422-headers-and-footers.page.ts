/**
 * GENERATED by apx-testgen from pages/p00422-headers-and-footers.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class HeadersAndFootersPage {
  static readonly alias = 'headers-and-footers';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, HeadersAndFootersPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** "Footer Example 1" */
  async clickFooter(): Promise<void> {
    await buttonByLabel(this.page, 'Footer Example 1').click();
  }

  /** "Footer Example 2" */
  async clickFooter2(): Promise<void> {
    await buttonByLabel(this.page, 'Footer Example 2').click();
  }

  /** "Header Example 1" */
  async clickHeader(): Promise<void> {
    await buttonByLabel(this.page, 'Header Example 1').click();
  }

  /** "Header Example 2" */
  async clickHeader2(): Promise<void> {
    await buttonByLabel(this.page, 'Header Example 2').click();
  }
}
