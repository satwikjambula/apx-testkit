/**
 * GENERATED by apx-testgen from pages/p03003-comments-component.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03003-comments-component.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, comments-b, comments-c, components-comments, configuration-basic, configuration-chat, configuration-chat_1, demo-basic, demo-chat, demo-thread, instructions, overview, region-display-selector, sample-sql-query
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), comments-b (themeTemplateComponent/comments), comments-c (themeTemplateComponent/comments), components-comments (themeTemplateComponent/comments), configuration-basic (staticContent), configuration-chat (staticContent), configuration-chat_1 (staticContent), demo-basic (staticContent), demo-chat (staticContent), demo-thread (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query (plugin/regionSourceCode).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { CommentsComponentPage } from './p03003-comments-component.page.js';

test.describe('page 3003: Components - Comments', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new CommentsComponentPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new CommentsComponentPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Comments - &APP_TITLE.'));
  });
});
