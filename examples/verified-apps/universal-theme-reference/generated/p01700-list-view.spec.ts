/**
 * GENERATED by apx-testgen from pages/p01700-list-view.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01700-list-view.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: 1-default, 2-with-list-divider-and-badge, breadcrumb, departments, examples, overview, projects-list, region-display-selector
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): 1-default (staticContent), 2-with-list-divider-and-badge (staticContent), breadcrumb (breadcrumb), departments (listView), examples (staticContent), overview (staticContent), projects-list (listView), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ListViewPage } from './p01700-list-view.page.js';

test.describe('page 1700: List View', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ListViewPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ListViewPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('List View'));
  });
});
