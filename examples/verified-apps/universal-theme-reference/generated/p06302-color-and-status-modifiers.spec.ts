/**
 * GENERATED by apx-testgen from pages/p06302-color-and-status-modifiers.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p06302-color-and-status-modifiers.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, general-color-utilities, general-utility-classes, opacity-class, opacity-utility-class, rds, shadow-class, shadow-utility-class, stateful-color-utilities, stateful-utility-classes
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), general-color-utilities (staticContent), general-utility-classes (classicReport), opacity-class (classicReport), opacity-utility-class (staticContent), rds (regionDisplaySelector), shadow-class (classicReport), shadow-utility-class (staticContent), stateful-color-utilities (staticContent), stateful-utility-classes (classicReport).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ColorAndStatusModifiersPage } from './p06302-color-and-status-modifiers.page.js';

test.describe('page 6302: Color and Status Modifiers', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ColorAndStatusModifiersPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ColorAndStatusModifiersPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Color and Status Modifiers - &APP_TITLE.'));
  });
});
