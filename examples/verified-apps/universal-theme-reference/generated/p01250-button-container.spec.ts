/**
 * GENERATED by apx-testgen from pages/p01250-button-container.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01250-button-container.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-container-demo, button-positions, button-positions-demo, demo, instructions, overview, region-display-selector, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-container-demo (staticContent), button-positions (staticContent), button-positions-demo (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ButtonContainerPage } from './p01250-button-container.page.js';

test.describe('page 1250: Button Container', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ButtonContainerPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ButtonContainerPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Button Container'));
  });

  test('every labeled .apx button exists (10 buttons)', async ({ page }) => {
    const po = new ButtonContainerPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Change', 'Close', 'Create', 'Delete', 'Edit', 'Next', 'Previous', 'Create', 'Next', 'Previous']);
  });
});
