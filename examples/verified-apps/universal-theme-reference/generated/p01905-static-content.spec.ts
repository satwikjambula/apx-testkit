/**
 * GENERATED by apx-testgen from pages/p01905-static-content.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01905-static-content.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, instructions, overview, region-display-selector, static-content
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), static-content (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { StaticContentPage } from './p01905-static-content.page.js';

test.describe('page 1905: Static Content', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new StaticContentPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new StaticContentPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Static Content - &APP_TITLE.'));
  });
});
