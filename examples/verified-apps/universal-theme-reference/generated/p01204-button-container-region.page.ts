/**
 * GENERATED by apx-testgen from pages/p01204-button-container-region.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ButtonContainerRegionPage {
  static readonly alias = 'button-container-region';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ButtonContainerRegionPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** radioGroup, "Radio Group" */
  get radioA(): ApexItem {
    return new ApexItem(this.page, 'P1204_RADIO_A');
  }

  /** radioGroup, "Radio Button Group" */
  get radioB(): ApexItem {
    return new ApexItem(this.page, 'P1204_RADIO_B');
  }

  /** "Previous" */
  async clickBtnGrp2a(): Promise<void> {
    await buttonByLabel(this.page, 'Previous').click();
  }

  /** "Rewind" */
  async clickBtnGrp2b(): Promise<void> {
    await buttonByLabel(this.page, 'Rewind').click();
  }

  /** "Play" */
  async clickBtnGrp2c(): Promise<void> {
    await buttonByLabel(this.page, 'Play').click();
  }

  /** "Stop" */
  async clickBtnGrp2d(): Promise<void> {
    await buttonByLabel(this.page, 'Stop').click();
  }

  /** "Forward" */
  async clickBtnGrp2e(): Promise<void> {
    await buttonByLabel(this.page, 'Forward').click();
  }

  /** "Next" */
  async clickBtnGrp2f(): Promise<void> {
    await buttonByLabel(this.page, 'Next').click();
  }

  /** "Button A" */
  async clickBtnGrpA(): Promise<void> {
    await buttonByLabel(this.page, 'Button A').click();
  }

  /** "Button B" */
  async clickBtnGrpB(): Promise<void> {
    await buttonByLabel(this.page, 'Button B').click();
  }

  /** "Button C" */
  async clickBtnGrpC(): Promise<void> {
    await buttonByLabel(this.page, 'Button C').click();
  }
}
