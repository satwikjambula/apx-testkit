/**
 * GENERATED by apx-testgen from pages/p03810-breadcrumb.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03810-breadcrumb.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: blank-region, breadcrumb, demo, instructions, overview, region-display-selector, title-bar, within-the-blank-with-attributes-template, within-the-title-bar-template
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): blank-region (breadcrumb), breadcrumb (breadcrumb), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), title-bar (breadcrumb), within-the-blank-with-attributes-template (staticContent), within-the-title-bar-template (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { BreadcrumbPage } from './p03810-breadcrumb.page.js';

test.describe('page 3810: Breadcrumb', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new BreadcrumbPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new BreadcrumbPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Breadcrumb - &APP_TITLE.'));
  });
});
