/**
 * GENERATED by apx-testgen from pages/p04000-icons.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class IconsPage {
  static readonly alias = 'icons';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, IconsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Animation" */
  get animation(): ApexItem {
    return new ApexItem(this.page, 'P4000_ANIMATION');
  }

  /** radioGroup, "Size" */
  get faSize(): ApexItem {
    return new ApexItem(this.page, 'P4000_FA_SIZE');
  }

  /** hidden */
  get iconclass(): ApexItem {
    return new ApexItem(this.page, 'P4000_ICONCLASS');
  }

  /** radioGroup, "Scale" */
  get iconSize(): ApexItem {
    return new ApexItem(this.page, 'P4000_ICON_SIZE');
  }

  /** selectList, "Modifier" */
  get modifier(): ApexItem {
    return new ApexItem(this.page, 'P4000_MODIFIER');
  }

  /** selectList, "Status" */
  get modifierStatus(): ApexItem {
    return new ApexItem(this.page, 'P4000_MODIFIER_STATUS');
  }

  /** selectList, "Rotate" */
  get rotate(): ApexItem {
    return new ApexItem(this.page, 'P4000_ROTATE');
  }

  /** textField, "Search Icons" */
  get search(): ApexItem {
    return new ApexItem(this.page, 'P4000_SEARCH');
  }

  /** radioGroup, "Size" */
  get size(): ApexItem {
    return new ApexItem(this.page, 'P4000_SIZE');
  }

  /** "About Font APEX" */
  async clickAbout(): Promise<void> {
    await buttonByLabel(this.page, 'About Font APEX').click();
  }

  /** "Reset Icon" */
  async clickResetIcon(): Promise<void> {
    await buttonByLabel(this.page, 'Reset Icon').click();
  }
}
