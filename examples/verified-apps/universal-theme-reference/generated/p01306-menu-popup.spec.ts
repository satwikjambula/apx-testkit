/**
 * GENERATED by apx-testgen from pages/p01306-menu-popup.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01306-menu-popup.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, customized-menu, customized-menu-2, instructions, menu-popup, multi-level-menu-example, overview, region-display-selector
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), customized-menu (staticContent), customized-menu-2 (list), instructions (staticContent), menu-popup (list), multi-level-menu-example (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { MenuPopupPage } from './p01306-menu-popup.page.js';

test.describe('page 1306: Menu Popup', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MenuPopupPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MenuPopupPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Menu Popup - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new MenuPopupPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Menu Button', 'Menu Button']);
  });
});
