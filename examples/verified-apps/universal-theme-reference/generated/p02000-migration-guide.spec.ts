/**
 * GENERATED by apx-testgen from pages/p02000-migration-guide.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p02000-migration-guide.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: backup-reminder, bookmarklet, breadcrumb, creating-the-theme, how-to-refresh, lost-subscriptions, migrating-from-other-themes, overview, post-migration, re-apply-your-theme-style, refresh-universal-theme, region-display-selector, restoring-subscription, starting-from-universal-theme, switching-the-theme
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): backup-reminder (staticContent), bookmarklet (staticContent), breadcrumb (breadcrumb), creating-the-theme (staticContent), how-to-refresh (staticContent), lost-subscriptions (staticContent), migrating-from-other-themes (staticContent), overview (staticContent), post-migration (staticContent), re-apply-your-theme-style (staticContent), refresh-universal-theme (staticContent), region-display-selector (regionDisplaySelector), restoring-subscription (staticContent), starting-from-universal-theme (staticContent), switching-the-theme (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { MigrationGuidePage } from './p02000-migration-guide.page.js';

test.describe('page 2000: Migration Guide', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MigrationGuidePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MigrationGuidePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Migration Guide - &APP_TITLE.'));
  });
});
