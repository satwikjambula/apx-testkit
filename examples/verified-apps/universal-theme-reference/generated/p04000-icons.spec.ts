/**
 * GENERATED by apx-testgen from pages/p04000-icons.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p04000-icons.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: about-font-apex, icon-builder, icon-builder-dialog, icon-form, icon-instructions, icon-preview, icons, icons-2, search
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): about-font-apex (staticContent), icon-builder (staticContent), icon-builder-dialog (staticContent), icon-form (staticContent), icon-instructions (staticContent), icon-preview (staticContent), icons (staticContent), icons-2 (staticContent), search (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { IconsPage } from './p04000-icons.page.js';

test.describe('page 4000: Icons', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new IconsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new IconsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Icons - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (9 items, 1 hidden)', async ({ page }) => {
    const po = new IconsPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P4000_ANIMATION', 'P4000_FA_SIZE', 'P4000_ICONCLASS', 'P4000_ICON_SIZE', 'P4000_MODIFIER', 'P4000_MODIFIER_STATUS', 'P4000_ROTATE', 'P4000_SEARCH', 'P4000_SIZE']);
  });

  test('apex.item round-trip on P4000_SEARCH', async ({ page }) => {
    const po = new IconsPage(page);
    await po.goto();
    await po.search.setValue('apx-testgen');
    expect(await po.search.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new IconsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['About Font APEX', 'Reset Icon']);
  });
});
