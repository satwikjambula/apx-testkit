/**
 * GENERATED by apx-testgen from pages/p01205-carousel-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01205-carousel-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-positions, carousel-region, carousel-region-with-buttons, demo, instructions, overview, region-a, region-a-2, region-b, region-b-2, region-c, region-c-2, region-display-selector, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-positions (staticContent), carousel-region (staticContent), carousel-region-with-buttons (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-a (staticContent), region-a-2 (staticContent), region-b (staticContent), region-b-2 (staticContent), region-c (staticContent), region-c-2 (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { CarouselRegionPage } from './p01205-carousel-region.page.js';

test.describe('page 1205: Region - Carousel', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new CarouselRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new CarouselRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Carousel Region - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (9 buttons)', async ({ page }) => {
    const po = new CarouselRegionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Change', 'Close', 'Copy', 'Create', 'Delete', 'Edit', 'Help', 'Next', 'Previous']);
  });
});
