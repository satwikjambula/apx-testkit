/**
 * GENERATED by apx-testgen from pages/p01915-inline-popup.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01915-inline-popup.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: auto-height-popup, breadcrumb, collapsible-content, demo, fixed-height-popup, overview, popup-with-callout, region-display-selector
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): auto-height-popup (staticContent), breadcrumb (breadcrumb), collapsible-content (staticContent), demo (staticContent), fixed-height-popup (staticContent), overview (staticContent), popup-with-callout (staticContent), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { InlinePopupPage } from './p01915-inline-popup.page.js';

test.describe('page 1915: Inline Popup', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new InlinePopupPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new InlinePopupPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Inline Popup'));
  });

  test('every labeled .apx button exists (8 buttons)', async ({ page }) => {
    const po = new InlinePopupPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Cancel', 'OK', 'OK', 'OK', 'Inline Popup with Auto Size', 'Inline Popup with Fixed Size', 'Inline Popup with Fixed Size']);
  });
});
