/**
 * GENERATED by apx-testgen from pages/p01908-dynamic-content-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01908-dynamic-content-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, demo-2, instructions, overview, region-display-selector, sample-code
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), demo-2 (dynamicContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), sample-code (plugin/regionSourceCode).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { DynamicContentRegionPage } from './p01908-dynamic-content-region.page.js';

test.describe('page 1908: Dynamic Content Region', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new DynamicContentRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new DynamicContentRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Dynamic Content Region- &APP_TITLE.'));
  });
});
