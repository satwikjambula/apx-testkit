/**
 * GENERATED by apx-testgen from pages/p01600-forms.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01600-forms.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, demo-2, demo-3, floating-labels, instructions, instructions-2, instructions-3, label-above, label-horizontal, overview, region-display-selector, template-options, template-options-2, template-options-3
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), demo-2 (staticContent), demo-3 (staticContent), floating-labels (staticContent), instructions (plugin/componentInstructions), instructions-2 (plugin/componentInstructions), instructions-3 (plugin/componentInstructions), label-above (staticContent), label-horizontal (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions), template-options-3 (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { FormsPage } from './p01600-forms.page.js';

test.describe('page 1600: Forms', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new FormsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new FormsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Forms - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (3 items, 0 hidden)', async ({ page }) => {
    const po = new FormsPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1600_ABOVE', 'P1600_FLOATING', 'P1600_HORIZONTAL']);
  });

  test('apex.item round-trip on P1600_ABOVE', async ({ page }) => {
    const po = new FormsPage(page);
    await po.goto();
    await po.above.setValue('apx-testgen');
    expect(await po.above.getValue()).toBe('apx-testgen');
  });
});
