/**
 * GENERATED by apx-testgen from pages/p01209-content-block.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01209-content-block.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-positions, content-block-region-template, demo, instructions, overview, overview-2, region-display-selector, region-title, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-positions (staticContent), content-block-region-template (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), overview-2 (staticContent), region-display-selector (regionDisplaySelector), region-title (staticContent), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ContentBlockPage } from './p01209-content-block.page.js';

test.describe('page 1209: Content Block', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ContentBlockPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ContentBlockPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Content Block'));
  });

  test('every labeled .apx button exists (4 buttons)', async ({ page }) => {
    const po = new ContentBlockPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Change', 'Edit', 'Next', 'Previous']);
  });
});
