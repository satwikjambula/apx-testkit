/**
 * GENERATED by apx-testgen from pages/p01411-reports-faceted-search.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01411-reports-faceted-search.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-bar, demo, filters, instructions, overview, region-display-selector, search-results, template-options
 * Region resolve-check emitted for 1 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-bar (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), search-results (classicReport), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle } from '@apx/testkit';
import { ReportsFacetedSearchPage } from './p01411-reports-faceted-search.page.js';

test.describe('page 1411: Reports - Faceted Search', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ReportsFacetedSearchPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ReportsFacetedSearchPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Faceted Search - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new ReportsFacetedSearchPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Reset']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (1 region)', async ({ page }) => {
    const po = new ReportsFacetedSearchPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['Demo1']);
  });
});
