/**
 * GENERATED by apx-testgen from pages/p01112-wizard-dialog-page.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01112-wizard-dialog-page.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: region-a, region-b, region-footer, wizard-progress
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): region-a (staticContent), region-b (staticContent), region-footer (staticContent), wizard-progress (list).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { WizardDialogPagePage } from './p01112-wizard-dialog-page.page.js';

test.describe('page 1112: Wizard Dialog Page', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new WizardDialogPagePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new WizardDialogPagePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Wizard Dialog Page - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new WizardDialogPagePage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Next', 'Previous']);
  });
});
