/**
 * GENERATED by apx-testgen from pages/p03005-media-list-component.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03005-media-list-component.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, components-media-list, configuration, demo, instructions, overview, region-display-selector, sample-sql-query
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), components-media-list (themeTemplateComponent/mediaList), configuration (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query (plugin/regionSourceCode).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { MediaListComponentPage } from './p03005-media-list-component.page.js';

test.describe('page 3005: Components - Media List', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MediaListComponentPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MediaListComponentPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Media List - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (1 items, 0 hidden)', async ({ page }) => {
    const po = new MediaListComponentPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P3005_ORDER_BY']);
  });
});
