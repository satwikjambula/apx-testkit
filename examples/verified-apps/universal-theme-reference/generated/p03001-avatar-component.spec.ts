/**
 * GENERATED by apx-testgen from pages/p03001-avatar-component.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03001-avatar-component.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: avatar, breadcrumb, classic-report, configuration-classic-report, configuration-interactive-report, configuration-multiple-report, demo-classic-report, demo-interactive-report, demo-multiple-report, interactive-report, overview, region-display-selector, sample-sql-query-classic-report, sample-sql-query-interactive-report, sample-sql-query-multiple-report
 * Region resolve-check emitted for 1 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): avatar (themeTemplateComponent/avatar), breadcrumb (breadcrumb), classic-report (classicReport), configuration-classic-report (staticContent), configuration-interactive-report (staticContent), configuration-multiple-report (staticContent), demo-classic-report (staticContent), demo-interactive-report (staticContent), demo-multiple-report (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query-classic-report (plugin/regionSourceCode), sample-sql-query-interactive-report (plugin/regionSourceCode), sample-sql-query-multiple-report (plugin/regionSourceCode).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectRegionsResolve, normalizeTitle } from '@apx/testkit';
import { AvatarComponentPage } from './p03001-avatar-component.page.js';

test.describe('page 3001: Components - Avatar', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new AvatarComponentPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new AvatarComponentPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Avatar - &APP_TITLE.'));
  });

  test('every interactiveReport/cards/facetedSearch region resolves (1 region)', async ({ page }) => {
    const po = new AvatarComponentPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['IRDemo']);
  });
});
