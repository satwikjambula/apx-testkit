/**
 * GENERATED by apx-testgen from pages/p00423-data-entry2.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00423-data-entry2.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, form-design, good-design-practices, mobile-form-example, overview
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), form-design (staticContent), good-design-practices (staticContent), mobile-form-example (staticContent), overview (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { DataEntry2Page } from './p00423-data-entry2.page.js';

test.describe('page 423: Data Entry', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new DataEntry2Page(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new DataEntry2Page(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Data Entry'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (12 items, 0 hidden)', async ({ page }) => {
    const po = new DataEntry2Page(page);
    await po.goto();
    await expectItemsPresent(page, ['P423_CHECKBOX_GROUP', 'P423_COLOR_PICKER', 'P423_DATE_PICKER', 'P423_FILE_BROWSE', 'P423_NUMBER_FIELD', 'P423_PASSWORD', 'P423_PERCENT_GRAPH', 'P423_RADIO_GROUP', 'P423_SELECT_LIST', 'P423_SWITCH', 'P423_TEXTAREA', 'P423_TEXT_FIELD']);
  });

  test('apex.item round-trip on P423_TEXTAREA', async ({ page }) => {
    const po = new DataEntry2Page(page);
    await po.goto();
    await po.textarea.setValue('apx-testgen');
    expect(await po.textarea.getValue()).toBe('apx-testgen');
  });
});
