/**
 * GENERATED by apx-testgen from pages/p01202-alert-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01202-alert-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: access-denied, alert-region, alert-region-2, alert-region-with-buttons, breadcrumb, button-positions, custom-icon, default, demo, instructions, order-created-successfully, overview, region-display-selector, template-options, template-options-2, use-cases
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): access-denied (staticContent), alert-region (staticContent), alert-region-2 (staticContent), alert-region-with-buttons (staticContent), breadcrumb (breadcrumb), button-positions (staticContent), custom-icon (staticContent), default (staticContent), demo (staticContent), instructions (plugin/componentInstructions), order-created-successfully (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions), use-cases (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { AlertRegionPage } from './p01202-alert-region.page.js';

test.describe('page 1202: Region - Alert', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new AlertRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new AlertRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Alert - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (7 buttons)', async ({ page }) => {
    const po = new AlertRegionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Close', 'Contact Administrator', 'Continue Shopping', 'Create', 'Next', 'Previous', 'View Order']);
  });
});
