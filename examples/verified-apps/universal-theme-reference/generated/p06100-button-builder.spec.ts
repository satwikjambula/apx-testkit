/**
 * GENERATED by apx-testgen from pages/p06100-button-builder.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p06100-button-builder.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: advanced, appearance, breadcrumb, builder, icon-picker, icons, preview, search, template-options, usage-instructions
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): advanced (staticContent), appearance (staticContent), breadcrumb (breadcrumb), builder (staticContent), icon-picker (staticContent), icons (staticContent), preview (staticContent), search (staticContent), template-options (staticContent), usage-instructions (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ButtonBuilderPage } from './p06100-button-builder.page.js';

test.describe('page 6100: Button Builder', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ButtonBuilderPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ButtonBuilderPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Button Builder - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (15 items, 0 hidden)', async ({ page }) => {
    const po = new ButtonBuilderPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P6100_BUTTON_LABEL', 'P6100_BUTTON_SET', 'P6100_BUTTON_TEMPLATE', 'P6100_HOT', 'P6100_ICON_CLASS', 'P6100_ICON_POSITION', 'P6100_SEARCH', 'P6100_SIZE', 'P6100_SPACING_BOTTOM', 'P6100_SPACING_LEFT', 'P6100_SPACING_RIGHT', 'P6100_SPACING_TOP', 'P6100_STYLE', 'P6100_TYPE', 'P6100_WIDTH']);
  });

  test('apex.item round-trip on P6100_BUTTON_LABEL', async ({ page }) => {
    const po = new ButtonBuilderPage(page);
    await po.goto();
    await po.buttonLabel.setValue('apx-testgen');
    expect(await po.buttonLabel.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new ButtonBuilderPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Icon dialog button']);
  });
});
