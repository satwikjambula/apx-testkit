/**
 * GENERATED by apx-testgen from pages/p01902-charts.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01902-charts.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: 1-bar, 2-dial-gauge, 3-donut, breadcrumb, demo, overview, region-display-selector, sample-sql-query-bar, sample-sql-query-dial-gauge, sample-sql-query-donut
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * 3 chart region(s) SKIPPED -- no htmlDomId set, runtime id genuinely unconstructible from static data (ADR-003 layer 3): 1-bar, 2-dial-gauge, 3-donut.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query-bar (staticContent), sample-sql-query-dial-gauge (staticContent), sample-sql-query-donut (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ChartsPage } from './p01902-charts.page.js';

test.describe('page 1902: Charts', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ChartsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ChartsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Charts - &APP_TITLE.'));
  });
});
