/**
 * GENERATED by apx-testgen from pages/p01114-login-page-template.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01114-login-page-template.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: login-region
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): login-region (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { LoginPageTemplatePage } from './p01114-login-page-template.page.js';

test.describe('page 1114: Login Page Template', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new LoginPageTemplatePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new LoginPageTemplatePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Login Page Template - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 0 hidden)', async ({ page }) => {
    const po = new LoginPageTemplatePage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1114_PASSWORD', 'P1114_USER']);
  });

  test('apex.item round-trip on P1114_USER', async ({ page }) => {
    const po = new LoginPageTemplatePage(page);
    await po.goto();
    await po.user.setValue('apx-testgen');
    expect(await po.user.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new LoginPageTemplatePage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Log In']);
  });
});
