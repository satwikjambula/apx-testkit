/**
 * GENERATED by apx-testgen from pages/p01907-tabs.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01907-tabs.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, demo-2, instructions, instructions-2, overview, region-display-selector, tab, tab-2, tab-3, tabs-as-a-list, tabs-as-regions, tabs-container, tabs-list, template-options, template-options-2
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), demo-2 (staticContent), instructions (plugin/componentInstructions), instructions-2 (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), tab (staticContent), tab-2 (staticContent), tab-3 (staticContent), tabs-as-a-list (staticContent), tabs-as-regions (staticContent), tabs-container (staticContent), tabs-list (list), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { TabsPage } from './p01907-tabs.page.js';

test.describe('page 1907: Tabs', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new TabsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new TabsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Tabs - &APP_TITLE.'));
  });
});
