/**
 * GENERATED by apx-testgen from pages/p01917-page-drawer.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01917-page-drawer.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, overview, region-display-selector, region-positions
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), region-positions (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { PageDrawerPage } from './p01917-page-drawer.page.js';

test.describe('page 1917: Page Drawer', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new PageDrawerPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new PageDrawerPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Page Drawer'));
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new PageDrawerPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Drawer with Auto Size', 'Drawer with Fixed Size']);
  });
});
