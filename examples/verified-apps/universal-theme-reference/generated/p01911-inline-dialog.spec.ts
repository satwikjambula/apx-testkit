/**
 * GENERATED by apx-testgen from pages/p01911-inline-dialog.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01911-inline-dialog.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: auto-height, breadcrumb, demo, fixed-height, overview, region-display-selector, toggle-region-content, toggle-region-content-2
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): auto-height (staticContent), breadcrumb (breadcrumb), demo (staticContent), fixed-height (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), toggle-region-content (staticContent), toggle-region-content-2 (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { InlineDialogPage } from './p01911-inline-dialog.page.js';

test.describe('page 1911: Inline Dialog', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new InlineDialogPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new InlineDialogPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Inline Dialog - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (6 buttons)', async ({ page }) => {
    const po = new InlineDialogPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Cancel', 'OK', 'OK', 'Inline Dialog with Auto Size', 'Inline Dialog with Fixed Size']);
  });
});
