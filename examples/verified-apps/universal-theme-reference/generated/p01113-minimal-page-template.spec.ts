/**
 * GENERATED by apx-testgen from pages/p01113-minimal-page-template.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01113-minimal-page-template.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, region-a, region-b, region-b-2
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), region-a (staticContent), region-b (staticContent), region-b-2 (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { MinimalPageTemplatePage } from './p01113-minimal-page-template.page.js';

test.describe('page 1113: Minimal Page Template', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MinimalPageTemplatePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MinimalPageTemplatePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Minimal Page Template - &APP_TITLE.'));
  });
});
