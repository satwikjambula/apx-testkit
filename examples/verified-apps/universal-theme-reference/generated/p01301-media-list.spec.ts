/**
 * GENERATED by apx-testgen from pages/p01301-media-list.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01301-media-list.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, column-aliases, demo, demo-2, instructions, instructions-2, list-attributes, media-list-as-a-list-template, media-list-as-a-report-template, overview, region-display-selector, sample-sql-query, template-options, template-options-2
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), column-aliases (classicReport), demo (list), demo-2 (classicReport), instructions (plugin/componentInstructions), instructions-2 (plugin/componentInstructions), list-attributes (classicReport), media-list-as-a-list-template (staticContent), media-list-as-a-report-template (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query (plugin/regionSourceCode), template-options (plugin/previewTemplateOptions), template-options-2 (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { MediaListPage } from './p01301-media-list.page.js';

test.describe('page 1301: "Lists - Media List "', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MediaListPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MediaListPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Media List - &APP_TITLE.'));
  });
});
