/**
 * GENERATED by apx-testgen from pages/p00424-touch-gestures.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00424-touch-gestures.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: available-gestures, breadcrumb, details, details-2, details-3, details-4, overview, pan, press, swipe, tap, touch-region, touch-region-2, touch-region-3, touch-region-4
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): available-gestures (staticContent), breadcrumb (breadcrumb), details (staticContent), details-2 (staticContent), details-3 (staticContent), details-4 (staticContent), overview (staticContent), pan (staticContent), press (staticContent), swipe (staticContent), tap (staticContent), touch-region (staticContent), touch-region-2 (staticContent), touch-region-3 (staticContent), touch-region-4 (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { TouchGesturesPage } from './p00424-touch-gestures.page.js';

test.describe('page 424: Touch Gestures', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new TouchGesturesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new TouchGesturesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Touch Gestures'));
  });
});
