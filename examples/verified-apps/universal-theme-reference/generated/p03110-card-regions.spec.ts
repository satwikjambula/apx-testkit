/**
 * GENERATED by apx-testgen from pages/p03110-card-regions.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03110-card-regions.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: all-settings-cards, backgrounds-cards, badges-cards, breadcrumb, configuration, configuration-2, configuration-3, configuration-4, configuration-5, demo, grid-layout-cards, image-first-cards, overview, region-display-selector, sample-sql-query, with-all-attributes-buttons-and-pagination, with-backgrounds, with-badges, with-grid-layout, with-image-first
 * Region resolve-check emitted for 5 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), configuration (staticContent), configuration-2 (staticContent), configuration-3 (staticContent), configuration-4 (staticContent), configuration-5 (staticContent), demo (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-sql-query (staticContent), with-all-attributes-buttons-and-pagination (staticContent), with-backgrounds (staticContent), with-badges (staticContent), with-grid-layout (staticContent), with-image-first (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectRegionsResolve, normalizeTitle } from '@apx/testkit';
import { CardRegionsPage } from './p03110-card-regions.page.js';

test.describe('page 3110: Card Regions', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new CardRegionsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new CardRegionsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Card Regions'));
  });

  test('every interactiveReport/cards/facetedSearch region resolves (5 regions)', async ({ page }) => {
    const po = new CardRegionsPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['all-settings-cards', 'backgrounds-cards', 'badges-cards', 'grid-layout-cards', 'image-first-cards']);
  });
});
