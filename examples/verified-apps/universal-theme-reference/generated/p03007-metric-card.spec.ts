/**
 * GENERATED by apx-testgen from pages/p03007-metric-card.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p03007-metric-card.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: MetricCardDemo, breadcrumb, configuration, configuration_1, configuration_2, default-layout, demo, inline-avatar-and-3-column-layout, new, new_1, new_2, overview, region-display-selector, stacked-layout
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): MetricCardDemo (plugin/componentInstructions), breadcrumb (breadcrumb), configuration (staticContent), configuration_1 (staticContent), configuration_2 (staticContent), default-layout (staticContent), demo (staticContent), inline-avatar-and-3-column-layout (staticContent), new (themeTemplateComponent/metricCard), new_1 (themeTemplateComponent/metricCard), new_2 (themeTemplateComponent/metricCard), overview (staticContent), region-display-selector (regionDisplaySelector), stacked-layout (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { MetricCardPage } from './p03007-metric-card.page.js';

test.describe('page 3007: Components: Metric Card', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new MetricCardPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new MetricCardPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Metric Card'));
  });
});
