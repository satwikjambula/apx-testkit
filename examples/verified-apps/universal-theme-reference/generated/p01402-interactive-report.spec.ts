/**
 * GENERATED by apx-testgen from pages/p01402-interactive-report.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01402-interactive-report.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-positions, demo, instructions, interactive-report-with-buttons, overview, region-display-selector, simple-interactive-report, template-options
 * Region resolve-check emitted for 2 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-positions (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle } from '@apx/testkit';
import { InteractiveReportPage } from './p01402-interactive-report.page.js';

test.describe('page 1402: Reports - Interactive Report', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new InteractiveReportPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new InteractiveReportPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Interactive Report - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new InteractiveReportPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Right of Search Bar']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (2 regions)', async ({ page }) => {
    const po = new InteractiveReportPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['interactive-report-with-buttons', 'Demo1']);
  });
});
