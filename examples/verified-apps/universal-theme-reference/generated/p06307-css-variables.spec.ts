/**
 * GENERATED by apx-testgen from pages/p06307-css-variables.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p06307-css-variables.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, color-palette-variables, color-variables, component-variables, overview, region-display-selector, sample-component-css, sample-component-preview, shadow-variables, stateful-color-variables
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), color-palette-variables (classicReport), color-variables (staticContent), component-variables (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), sample-component-css (staticContent), sample-component-preview (staticContent), shadow-variables (staticContent), stateful-color-variables (classicReport).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { CssVariablesPage } from './p06307-css-variables.page.js';

test.describe('page 6307: CSS Variables', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new CssVariablesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new CssVariablesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('CSS Variables'));
  });
});
