/**
 * GENERATED by apx-testgen from pages/p06400-template-directives.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p06400-template-directives.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, case-directives, evaluation, example, example-2, example-3, example-4, if-directives, loop-directives, overview, rds, syntax, syntax-2, syntax-3, syntax-4, token-modifiers, with-apply-directives
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), case-directives (staticContent), evaluation (staticContent), example (staticContent), example-2 (staticContent), example-3 (staticContent), example-4 (staticContent), if-directives (staticContent), loop-directives (staticContent), overview (staticContent), rds (regionDisplaySelector), syntax (staticContent), syntax-2 (staticContent), syntax-3 (staticContent), syntax-4 (staticContent), token-modifiers (staticContent), with-apply-directives (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { TemplateDirectivesPage } from './p06400-template-directives.page.js';

test.describe('page 6400: Template Directives', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new TemplateDirectivesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new TemplateDirectivesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Template Directives'));
  });
});
