/**
 * GENERATED by apx-testgen from pages/p00405-theme-styles.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00405-theme-styles.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, official-theme-styles, official-theme-styles-cards, overview, region-display-selector
 * Region resolve-check emitted for 1 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), official-theme-styles (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectRegionsResolve, normalizeTitle } from '@apx/testkit';
import { ThemeStylesPage } from './p00405-theme-styles.page.js';

test.describe('page 405: Theme Styles', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ThemeStylesPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ThemeStylesPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Theme Styles'));
  });

  test('every interactiveReport/cards/facetedSearch region resolves (1 region)', async ({ page }) => {
    const po = new ThemeStylesPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['official-theme-styles-cards']);
  });
});
