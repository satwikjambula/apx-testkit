/**
 * GENERATED by apx-testgen from pages/p01207-title-bar-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01207-title-bar-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-positions, demo, instructions, overview, region-display-selector, template-options, title-bar-title, title-bar-with-buttons
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-positions (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), template-options (plugin/previewTemplateOptions), title-bar-title (staticContent), title-bar-with-buttons (breadcrumb).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { TitleBarRegionPage } from './p01207-title-bar-region.page.js';

test.describe('page 1207: Region - Title Bar', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new TitleBarRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new TitleBarRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Title Bar Region - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (9 buttons)', async ({ page }) => {
    const po = new TitleBarRegionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Change', 'Close', 'Copy', 'Create', 'Delete', 'Edit', 'Help', 'Next', 'Previous']);
  });
});
