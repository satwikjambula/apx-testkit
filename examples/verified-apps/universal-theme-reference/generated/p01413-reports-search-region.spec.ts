/**
 * GENERATED by apx-testgen from pages/p01413-reports-search-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01413-reports-search-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, demo, instructions, overview, region-display-selector, search-region, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), region-display-selector (regionDisplaySelector), search-region (search), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ReportsSearchRegionPage } from './p01413-reports-search-region.page.js';

test.describe('page 1413: Reports - Search Region', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ReportsSearchRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ReportsSearchRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Search Region - &APP_TITLE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (1 items, 0 hidden)', async ({ page }) => {
    const po = new ReportsSearchRegionPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1413_SEARCH_FIELD']);
  });

  test('apex.item round-trip on P1413_SEARCH_FIELD', async ({ page }) => {
    const po = new ReportsSearchRegionPage(page);
    await po.goto();
    await po.searchField.setValue('apx-testgen');
    expect(await po.searchField.getValue()).toBe('apx-testgen');
  });
});
