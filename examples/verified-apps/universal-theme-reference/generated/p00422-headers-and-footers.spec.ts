/**
 * GENERATED by apx-testgen from pages/p00422-headers-and-footers.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00422-headers-and-footers.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, mobile-page-footer, mobile-page-header, overview, page-footer-examples, page-header-examples
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), mobile-page-footer (staticContent), mobile-page-header (staticContent), overview (staticContent), page-footer-examples (staticContent), page-header-examples (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { HeadersAndFootersPage } from './p00422-headers-and-footers.page.js';

test.describe('page 422: Headers and Footers', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new HeadersAndFootersPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new HeadersAndFootersPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Headers and Footers'));
  });

  test('every labeled .apx button exists (4 buttons)', async ({ page }) => {
    const po = new HeadersAndFootersPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Footer Example 1', 'Footer Example 2', 'Header Example 1', 'Header Example 2']);
  });
});
