/**
 * GENERATED by apx-testgen from pages/p01201-standard-region.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p01201-standard-region.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, button-positions, demo, instructions, overview, overview-2, region-display-selector, region-title, standard-region-template, template-options
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), button-positions (staticContent), demo (staticContent), instructions (plugin/componentInstructions), overview (staticContent), overview-2 (staticContent), region-display-selector (regionDisplaySelector), region-title (staticContent), standard-region-template (staticContent), template-options (plugin/previewTemplateOptions).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { StandardRegionPage } from './p01201-standard-region.page.js';

test.describe('page 1201: Regions - Standard', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new StandardRegionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new StandardRegionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Standard Region - &APP_TITLE.'));
  });

  test('every labeled .apx button exists (9 buttons)', async ({ page }) => {
    const po = new StandardRegionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Change', 'Close', 'Copy', 'Create', 'Delete', 'Edit', 'Help', 'Next', 'Previous']);
  });
});
