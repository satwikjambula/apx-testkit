/**
 * GENERATED by apx-testgen from pages/p00015-line.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class LinePage {
  static readonly alias = 'line';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, LinePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** "Area" */
  async clickArea(): Promise<void> {
    await buttonByLabel(this.page, 'Area').click();
  }

  /** "Bar" */
  async clickBar(): Promise<void> {
    await buttonByLabel(this.page, 'Bar').click();
  }

  /** "Combo" */
  async clickCombo(): Promise<void> {
    await buttonByLabel(this.page, 'Combo').click();
  }

  /** "Hide Labels" */
  async clickHidelabels(): Promise<void> {
    await buttonByLabel(this.page, 'Hide Labels').click();
  }

  /** "Horizontal" */
  async clickHorizontal(): Promise<void> {
    await buttonByLabel(this.page, 'Horizontal').click();
  }

  /** "Line" */
  async clickLine(): Promise<void> {
    await buttonByLabel(this.page, 'Line').click();
  }

  /** "Line with Area" */
  async clickLinewitharea(): Promise<void> {
    await buttonByLabel(this.page, 'Line with Area').click();
  }

  /** "Reset Zoom" */
  async clickReset(): Promise<void> {
    await buttonByLabel(this.page, 'Reset Zoom').click();
  }

  /** "Show Labels" */
  async clickShowlabels(): Promise<void> {
    await buttonByLabel(this.page, 'Show Labels').click();
  }

  /** "Vertical" */
  async clickVertical(): Promise<void> {
    await buttonByLabel(this.page, 'Vertical').click();
  }
}
