/**
 * GENERATED by apx-testgen from pages/p00028-reference.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V5), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00028-reference.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: reference, reference-2
 * TODO(region-contract): emit region assertions once REGION DISCOVERY report
 * establishes the DOM convention for region static ids.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ReferencePage } from './p00028-reference.page.js';

test.describe('page 28: Reference', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ReferencePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ReferencePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Reference'));
  });
});
