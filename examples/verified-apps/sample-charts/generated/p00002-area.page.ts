/**
 * GENERATED by apx-testgen from pages/p00002-area.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class AreaPage {
  static readonly alias = 'area';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, AreaPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** "Curved" */
  async clickCurved(): Promise<void> {
    await buttonByLabel(this.page, 'Curved').click();
  }

  /** "Horizontal" */
  async clickHorizontal(): Promise<void> {
    await buttonByLabel(this.page, 'Horizontal').click();
  }

  /** "None" */
  async clickNone(): Promise<void> {
    await buttonByLabel(this.page, 'None').click();
  }

  /** "Centered Segmented" */
  async clickSegmented(): Promise<void> {
    await buttonByLabel(this.page, 'Centered Segmented').click();
  }

  /** "Stack" */
  async clickStack(): Promise<void> {
    await buttonByLabel(this.page, 'Stack').click();
  }

  /** "Stepped" */
  async clickStepped(): Promise<void> {
    await buttonByLabel(this.page, 'Stepped').click();
  }

  /** "Straight" */
  async clickStraight(): Promise<void> {
    await buttonByLabel(this.page, 'Straight').click();
  }

  /** "Unstack" */
  async clickUnstack(): Promise<void> {
    await buttonByLabel(this.page, 'Unstack').click();
  }

  /** "Vertical" */
  async clickVertical(): Promise<void> {
    await buttonByLabel(this.page, 'Vertical').click();
  }
}
