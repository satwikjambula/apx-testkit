/**
 * GENERATED by apx-testgen from pages/p00004-pie.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class PiePage {
  static readonly alias = 'pie';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, PiePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get productId(): ApexItem {
    return new ApexItem(this.page, 'P4_PRODUCT_ID');
  }

  /** displayOnly, "Product:" */
  get productName(): ApexItem {
    return new ApexItem(this.page, 'P4_PRODUCT_NAME');
  }

  /** "Ascending" */
  async clickAscending(): Promise<void> {
    await buttonByLabel(this.page, 'Ascending').click();
  }

  /** "Decrease Legend Size" */
  async clickDecrease(): Promise<void> {
    await buttonByLabel(this.page, 'Decrease Legend Size').click();
  }

  /** "Descending" */
  async clickDescending(): Promise<void> {
    await buttonByLabel(this.page, 'Descending').click();
  }

  /** "Increase Legend Size" */
  async clickIncrease(): Promise<void> {
    await buttonByLabel(this.page, 'Increase Legend Size').click();
  }
}
