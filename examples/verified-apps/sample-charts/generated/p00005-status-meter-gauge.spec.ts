/**
 * GENERATED by apx-testgen from pages/p00005-status-meter-gauge.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00005-status-meter-gauge.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: about-this-page, breadcrumbs, circular, horizontal, indicator-size, indicator-size-2, indicator-size-3, indicator-size-information, indicator-size-information-2, inner-radius, inner-radius-information, label-and-value, label-and-value-2, label-and-value-information, number-formatting, number-formatting-2, number-formatting-3, number-formatting-information, number-formatting-information-2, percent-formatting, percent-formatting-2, percent-formatting-information, plot-area-and-indicator-size, plot-area-and-indicator-size-2, plot-area-and-indicator-size-information, region-display-selector, start-angle-and-angle-extent, start-angle-and-angle-extent-information, thresholds, thresholds-2, thresholds-current-threshold-only, thresholds-current-threshold-only-2, thresholds-current-threshold-only-information, thresholds-information, thresholds-information-2, thresholds-on-indicator, thresholds-on-indicator-2, thresholds-on-indicator-information, thresholds-using-custom-tooltip, value-and-label-information, value-and-label-positioned-in-center, value-information, value-position, value-position-2, vertical
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * 24 chart region(s) SKIPPED -- no htmlDomId set, runtime id genuinely unconstructible from static data (ADR-003 layer 3): indicator-size, indicator-size-2, indicator-size-3, inner-radius, label-and-value, label-and-value-2, number-formatting, number-formatting-2, number-formatting-3, percent-formatting, percent-formatting-2, plot-area-and-indicator-size, plot-area-and-indicator-size-2, start-angle-and-angle-extent, thresholds, thresholds-2, thresholds-current-threshold-only, thresholds-current-threshold-only-2, thresholds-on-indicator, thresholds-on-indicator-2, thresholds-using-custom-tooltip, value-and-label-positioned-in-center, value-position, value-position-2.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): about-this-page (staticContent), breadcrumbs (breadcrumb), circular (staticContent), horizontal (staticContent), indicator-size-information (staticContent), indicator-size-information-2 (staticContent), inner-radius-information (staticContent), label-and-value-information (staticContent), number-formatting-information (staticContent), number-formatting-information-2 (staticContent), percent-formatting-information (staticContent), plot-area-and-indicator-size-information (staticContent), region-display-selector (regionDisplaySelector), start-angle-and-angle-extent-information (staticContent), thresholds-current-threshold-only-information (staticContent), thresholds-information (staticContent), thresholds-information-2 (staticContent), thresholds-on-indicator-information (staticContent), value-and-label-information (staticContent), value-information (staticContent), vertical (staticContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle, login } from '@apx/testkit';
import { StatusMeterGaugePage } from './p00005-status-meter-gauge.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 5: Status Meter Gauge [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new StatusMeterGaugePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new StatusMeterGaugePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Status Meter Gauge'));
  });
});
