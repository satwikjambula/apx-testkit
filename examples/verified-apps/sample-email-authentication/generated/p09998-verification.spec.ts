/**
 * GENERATED by apx-testgen from pages/p09998-verification.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p09998-verification.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: account-locked, for-cancel, for-token, token-maxed, user-verification
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): account-locked (staticContent), for-cancel (staticContent), for-token (staticContent), token-maxed (staticContent), user-verification (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { VerificationPage } from './p09998-verification.page.js';

test.describe('page 9998: User Verification', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new VerificationPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new VerificationPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Verification'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (5 items, 3 hidden)', async ({ page }) => {
    const po = new VerificationPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P9998_CODE', 'P9998_IS_ACCOUNT_LOCKED_YN', 'P9998_IS_TOKEN_MAXED_YN', 'P9998_RESET_VERIFY_AFTER_X_HOURS', 'P9998_USERNAME']);
  });

  test('apex.item round-trip on P9998_CODE', async ({ page }) => {
    const po = new VerificationPage(page);
    await po.goto();
    await po.code.setValue('apx-testgen');
    expect(await po.code.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new VerificationPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Verify']);
  });
});
