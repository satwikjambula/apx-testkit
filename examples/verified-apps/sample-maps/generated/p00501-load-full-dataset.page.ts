/**
 * GENERATED by apx-testgen from pages/p00501-load-full-dataset.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class LoadFullDatasetPage {
  static readonly alias = 'load-full-dataset';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, LoadFullDatasetPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** fileUpload, "Airports Data" */
  get airportsFile(): ApexItem {
    return new ApexItem(this.page, 'P501_AIRPORTS_FILE');
  }

  /** textField, "Use Proxy Server" */
  get proxyOverride(): ApexItem {
    return new ApexItem(this.page, 'P501_PROXY_OVERRIDE');
  }

  /** radioGroup, "Load Data From" */
  get source(): ApexItem {
    return new ApexItem(this.page, 'P501_SOURCE');
  }

  /** fileUpload, "US States Data" */
  get statesFile(): ApexItem {
    return new ApexItem(this.page, 'P501_STATES_FILE');
  }

  /** "Load Dataset" */
  async clickLoad(): Promise<void> {
    await buttonByLabel(this.page, 'Load Dataset').click();
  }
}
