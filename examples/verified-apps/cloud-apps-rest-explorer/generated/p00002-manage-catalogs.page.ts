/**
 * GENERATED by apx-testgen from pages/p00002-manage-catalogs.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ManageCatalogsPage {
  static readonly alias = 'manage-catalogs';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ManageCatalogsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get backgroundContext(): ApexItem {
    return new ApexItem(this.page, 'P2_BACKGROUND_CONTEXT');
  }

  /** hidden */
  get errorMessage(): ApexItem {
    return new ApexItem(this.page, 'P2_ERROR_MESSAGE');
  }

  /** selectList, "Release" */
  get release(): ApexItem {
    return new ApexItem(this.page, 'P2_RELEASE');
  }

  /** hidden */
  get selectedAction(): ApexItem {
    return new ApexItem(this.page, 'P2_SELECTED_ACTION');
  }

  /** hidden */
  get selectedPillar(): ApexItem {
    return new ApexItem(this.page, 'P2_SELECTED_PILLAR');
  }

  /** hidden */
  get selectedRelease(): ApexItem {
    return new ApexItem(this.page, 'P2_SELECTED_RELEASE');
  }
}
