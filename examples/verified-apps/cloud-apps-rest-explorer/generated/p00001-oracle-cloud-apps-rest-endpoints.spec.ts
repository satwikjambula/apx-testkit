/**
 * GENERATED by apx-testgen from pages/p00001-oracle-cloud-apps-rest-endpoints.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00001-oracle-cloud-apps-rest-endpoints.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: all-endpoints, background-progress, business-object-attributes, business-object-details, business-object-details-container, business-object-search, business-objects-list, business-objects-results, business-objects-tab, business-objects-tree, endpoints-in-use, help, oracle-cloud-apps-rest-describe-explorer, show-progress, tabs
 * Region resolve-check emitted for 2 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): all-endpoints (staticContent), background-progress (staticContent), business-object-details (themeTemplateComponent/contentRow), business-object-details-container (staticContent), business-object-search (smartFilters), business-objects-list (themeTemplateComponent/contentRow), business-objects-results (staticContent), business-objects-tab (staticContent), business-objects-tree (tree), help (staticContent), oracle-cloud-apps-rest-describe-explorer (staticContent), show-progress (classicReport), tabs (staticContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle, login } from '@apx/testkit';
import { OracleCloudAppsRestEndpointsPage } from './p00001-oracle-cloud-apps-rest-endpoints.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 1: Oracle Cloud Apps REST Endpoints [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new OracleCloudAppsRestEndpointsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new OracleCloudAppsRestEndpointsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Oracle Cloud Apps REST Endpoints'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (5 items, 4 hidden)', async ({ page }) => {
    const po = new OracleCloudAppsRestEndpointsPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1_INGEST_APPLICATION_ID', 'P1_INGEST_MODULE_STATIC_ID', 'P1_RELEASE_ID', 'P1_SELECTED_BO_ID', 'P1_TAB_ACTIVATED']);
  });

  test('every labeled .apx button exists (6 buttons)', async ({ page }) => {
    const po = new OracleCloudAppsRestEndpointsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Close', 'Download Remaining Service Descriptions In Background', 'Help', 'Manage Catalogs', 'Refresh List', 'Refresh Progress Display']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (2 regions)', async ({ page }) => {
    const po = new OracleCloudAppsRestEndpointsPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['all-endpoints-tab', 'fa-rest-endpoints-tab']);
  });
});
