/**
 * GENERATED by apx-testgen from pages/p00009-add-line-item.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class AddLineItemPage {
  static readonly alias = 'add-line-item';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, AddLineItemPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get lineId(): ApexItem {
    return new ApexItem(this.page, 'P9_LINE_ID');
  }

  /** hidden */
  get poId(): ApexItem {
    return new ApexItem(this.page, 'P9_PO_ID');
  }

  /** selectOne, "Product" */
  get productId(): ApexItem {
    return new ApexItem(this.page, 'P9_PRODUCT_ID');
  }

  /** numberField, "Quantity" */
  get quantity(): ApexItem {
    return new ApexItem(this.page, 'P9_QUANTITY');
  }

  /** displayOnly, "Total Amount" */
  get total(): ApexItem {
    return new ApexItem(this.page, 'P9_TOTAL');
  }

  /** numberField, "Unit Price" */
  get unitPrice(): ApexItem {
    return new ApexItem(this.page, 'P9_UNIT_PRICE');
  }

  /** "Cancel" */
  async clickCancelAddLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Create" */
  async clickCreateAddLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Create').click();
  }

  /** "Delete" */
  async clickDeleteAddLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply Changes" */
  async clickSaveAddLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
