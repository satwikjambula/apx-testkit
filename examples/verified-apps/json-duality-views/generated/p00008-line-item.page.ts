/**
 * GENERATED by apx-testgen from pages/p00008-line-item.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class LineItemPage {
  static readonly alias = 'line-item';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, LineItemPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get lineId(): ApexItem {
    return new ApexItem(this.page, 'P8_LINE_ID');
  }

  /** hidden */
  get poId(): ApexItem {
    return new ApexItem(this.page, 'P8_PO_ID');
  }

  /** textField, "Product Description" */
  get productDesc(): ApexItem {
    return new ApexItem(this.page, 'P8_PRODUCT_DESC');
  }

  /** textField, "Product Code" */
  get productProductCode(): ApexItem {
    return new ApexItem(this.page, 'P8_PRODUCT_PRODUCT_CODE');
  }

  /** hidden */
  get productUnitPrice(): ApexItem {
    return new ApexItem(this.page, 'P8_PRODUCT_UNIT_PRICE');
  }

  /** numberField, "Quantity" */
  get quantity(): ApexItem {
    return new ApexItem(this.page, 'P8_QUANTITY');
  }

  /** displayOnly, "Total Amount" */
  get total(): ApexItem {
    return new ApexItem(this.page, 'P8_TOTAL');
  }

  /** numberField, "Unit Price" */
  get unitPrice(): ApexItem {
    return new ApexItem(this.page, 'P8_UNIT_PRICE');
  }

  /** "Cancel" */
  async clickCancelLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Create" */
  async clickCreateLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Create').click();
  }

  /** "Delete" */
  async clickDeleteLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply" */
  async clickSaveLineItemButton(): Promise<void> {
    await buttonByLabel(this.page, 'Apply').click();
  }
}
