/**
 * GENERATED by apx-testgen from pages/p00016-multi-filetypes-load.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class MultiFiletypesLoadPage {
  static readonly alias = 'multi-filetypes-load';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, MultiFiletypesLoadPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get errorRowCount(): ApexItem {
    return new ApexItem(this.page, 'P16_ERROR_ROW_COUNT');
  }

  /** fileUpload, "Upload a File" */
  get file(): ApexItem {
    return new ApexItem(this.page, 'P16_FILE');
  }

  /** displayOnly, "Loaded File" */
  get fileName(): ApexItem {
    return new ApexItem(this.page, 'P16_FILE_NAME');
  }

  /** hidden */
  get fileType(): ApexItem {
    return new ApexItem(this.page, 'P16_FILE_TYPE');
  }

  /** hidden */
  get processedRowCount(): ApexItem {
    return new ApexItem(this.page, 'P16_PROCESSED_ROW_COUNT');
  }

  /** selectList, "XLSX Worksheet" */
  get xlsxWorksheet(): ApexItem {
    return new ApexItem(this.page, 'P16_XLSX_WORKSHEET');
  }

  /** "Clear" */
  async clickClear(): Promise<void> {
    await buttonByLabel(this.page, 'Clear').click();
  }

  /** "Load Data" */
  async clickLoad(): Promise<void> {
    await buttonByLabel(this.page, 'Load Data').click();
  }
}
