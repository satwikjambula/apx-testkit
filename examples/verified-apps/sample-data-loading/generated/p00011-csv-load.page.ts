/**
 * GENERATED by apx-testgen from pages/p00011-csv-load.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class CsvLoadPage {
  static readonly alias = 'csv-load';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, CsvLoadPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get copyPasteErrorRowCount(): ApexItem {
    return new ApexItem(this.page, 'P11_COPY_PASTE_ERROR_ROW_COUNT');
  }

  /** textarea, "Copy and Paste Delimited Data" */
  get data(): ApexItem {
    return new ApexItem(this.page, 'P11_DATA');
  }

  /** fileUpload, "Upload a File" */
  get file(): ApexItem {
    return new ApexItem(this.page, 'P11_FILE');
  }

  /** hidden */
  get fileErrorRowCount(): ApexItem {
    return new ApexItem(this.page, 'P11_FILE_ERROR_ROW_COUNT');
  }

  /** displayOnly, "Loaded File" */
  get fileName(): ApexItem {
    return new ApexItem(this.page, 'P11_FILE_NAME');
  }

  /** displayOnly, "Pasted Data" */
  get pastedData(): ApexItem {
    return new ApexItem(this.page, 'P11_PASTED_DATA');
  }

  /** "Clear" */
  async clickClearData(): Promise<void> {
    await buttonByLabel(this.page, 'Clear').click();
  }

  /** "Clear" */
  async clickClearFile(): Promise<void> {
    await buttonByLabel(this.page, 'Clear').click();
  }

  /** "Load Data" */
  async clickLoadData(): Promise<void> {
    await buttonByLabel(this.page, 'Load Data').click();
  }

  /** "Load Data" */
  async clickLoadFile(): Promise<void> {
    await buttonByLabel(this.page, 'Load Data').click();
  }

  /** "Next" */
  async clickNext(): Promise<void> {
    await buttonByLabel(this.page, 'Next').click();
  }
}
