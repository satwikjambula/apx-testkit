/**
 * GENERATED by apx-testgen from pages/p00017-background-load.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class BackgroundLoadPage {
  static readonly alias = 'background-load';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, BackgroundLoadPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get errorRows(): ApexItem {
    return new ApexItem(this.page, 'P17_ERROR_ROWS');
  }

  /** fileUpload, "Upload a File" */
  get file(): ApexItem {
    return new ApexItem(this.page, 'P17_FILE');
  }

  /** displayOnly, "Loaded File" */
  get fileName(): ApexItem {
    return new ApexItem(this.page, 'P17_FILE_NAME');
  }

  /** hidden */
  get loadExecId(): ApexItem {
    return new ApexItem(this.page, 'P17_LOAD_EXEC_ID');
  }

  /** hidden */
  get processedRows(): ApexItem {
    return new ApexItem(this.page, 'P17_PROCESSED_ROWS');
  }

  /** "Clear" */
  async clickClear(): Promise<void> {
    await buttonByLabel(this.page, 'Clear').click();
  }

  /** "Load Data" */
  async clickLoad(): Promise<void> {
    await buttonByLabel(this.page, 'Load Data').click();
  }
}
