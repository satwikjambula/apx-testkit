/**
 * GENERATED by apx-testgen from pages/p10000-admin.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p10000-admin.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: access-control, access-control-actions, access-control-content, acl-information, administration, approval-options, column, column-2, configuration, feedback, feedback-2, look-up-values, monitoring, nomenclature-project-interactions, notifications, report, user-counts-report, utilities, warning
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): access-control (staticContent), access-control-actions (list), access-control-content (staticContent), acl-information (plSqlDynamicContent), administration (breadcrumb), approval-options (list), column (staticContent), column-2 (staticContent), configuration (list), feedback (staticContent), feedback-2 (list), look-up-values (list), monitoring (list), nomenclature-project-interactions (list), notifications (list), report (classicReport), user-counts-report (classicReport), utilities (list), warning (plSqlDynamicContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle, login } from '@apx/testkit';
import { AdminPage } from './p10000-admin.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 10000: Administration [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new AdminPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new AdminPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Administration'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (17 items, 17 hidden)', async ({ page }) => {
    const po = new AdminPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P10000_ACTIVITY_TYPES', 'P10000_COMPETENCIES', 'P10000_COUNTRIES', 'P10000_DEFAULT_TAGS', 'P10000_FOCUS_AREAS', 'P10000_PROJECT_PRIORITIES', 'P10000_PROJECT_SIZES', 'P10000_PROJECT_STATUSES', 'P10000_PROJECT_STATUS_SCALES', 'P10000_REL_MILESTONE_TYPES', 'P10000_RESOURCE_TYPES', 'P10000_RM_DEFAULT_TAGS', 'P10000_SYSTEM_NOTIFICATIONS', 'P10000_TASK_STATUSES', 'P10000_TASK_TYPES', 'P10000_TEAM_MBR_DEFAULT_TAGS', 'P10000_USER_COUNTS']);
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new AdminPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Add User', 'Navigate Up']);
  });
});
