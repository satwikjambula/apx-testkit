/**
 * GENERATED by apx-testgen from pages/p00063-copy-release.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class CopyReleasePage {
  static readonly alias = 'copy-release';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, CopyReleasePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Copy From Release", required */
  get copyFromReleaseId(): ApexItem {
    return new ApexItem(this.page, 'P63_COPY_FROM_RELEASE_ID');
  }

  /** hidden */
  get newReleaseId(): ApexItem {
    return new ApexItem(this.page, 'P63_NEW_RELEASE_ID');
  }

  /** hidden */
  get openDate(): ApexItem {
    return new ApexItem(this.page, 'P63_OPEN_DATE');
  }

  /** numberField, "Push Days", required */
  get pushDays(): ApexItem {
    return new ApexItem(this.page, 'P63_PUSH_DAYS');
  }

  /** textField, "New Release", required */
  get release(): ApexItem {
    return new ApexItem(this.page, 'P63_RELEASE');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Copy" */
  async clickCopy(): Promise<void> {
    await buttonByLabel(this.page, 'Copy').click();
  }
}
