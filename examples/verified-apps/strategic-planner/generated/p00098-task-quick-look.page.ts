/**
 * GENERATED by apx-testgen from pages/p00098-task-quick-look.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class TaskQuickLookPage {
  static readonly alias = 'task-quick-look';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, TaskQuickLookPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get projectId(): ApexItem {
    return new ApexItem(this.page, 'P98_PROJECT_ID');
  }

  /** hidden */
  get taskId(): ApexItem {
    return new ApexItem(this.page, 'P98_TASK_ID');
  }

  /** hidden */
  get taskType(): ApexItem {
    return new ApexItem(this.page, 'P98_TASK_TYPE');
  }

  /** "Close" */
  async clickClose(): Promise<void> {
    await buttonByLabel(this.page, 'Close').click();
  }

  /** "View Details" */
  async clickDetails(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }

  /** "Edit" */
  async clickEdit(): Promise<void> {
    await buttonByLabel(this.page, 'Edit').click();
  }

  /** "Edit" */
  async clickEditMilestone(): Promise<void> {
    await buttonByLabel(this.page, 'Edit').click();
  }

  /** "Edit" */
  async clickEditReview(): Promise<void> {
    await buttonByLabel(this.page, 'Edit').click();
  }

  /** "History" */
  async clickTaskHistory(): Promise<void> {
    await buttonByLabel(this.page, 'History').click();
  }
}
