/**
 * GENERATED by apx-testgen from pages/p00203-release-calendar.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ReleaseCalendarPage {
  static readonly alias = 'release-calendar';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ReleaseCalendarPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get release(): ApexItem {
    return new ApexItem(this.page, 'P203_RELEASE');
  }

  /** hidden */
  get releaseId(): ApexItem {
    return new ApexItem(this.page, 'P203_RELEASE_ID');
  }

  /** "Report View" */
  async clickReportView(): Promise<void> {
    await buttonByLabel(this.page, 'Report View').click();
  }

  /** "Reset" */
  async clickReset(): Promise<void> {
    await buttonByLabel(this.page, 'Reset').click();
  }

  /** "Navigate Up" */
  async clickUp(): Promise<void> {
    await buttonByLabel(this.page, 'Navigate Up').click();
  }
}
