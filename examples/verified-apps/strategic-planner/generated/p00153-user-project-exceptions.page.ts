/**
 * GENERATED by apx-testgen from pages/p00153-user-project-exceptions.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class UserProjectExceptionsPage {
  static readonly alias = 'user-project-exceptions';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, UserProjectExceptionsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get email(): ApexItem {
    return new ApexItem(this.page, 'P153_EMAIL');
  }

  /** hidden */
  get firstName(): ApexItem {
    return new ApexItem(this.page, 'P153_FIRST_NAME');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P153_ID');
  }

  /** hidden */
  get lastName(): ApexItem {
    return new ApexItem(this.page, 'P153_LAST_NAME');
  }

  /** "Email to me" */
  async clickEmailMeExcept(): Promise<void> {
    await buttonByLabel(this.page, 'Email to me').click();
  }

  /** "Subscribe me" */
  async clickSubscribeMeExcept(): Promise<void> {
    await buttonByLabel(this.page, 'Subscribe me').click();
  }

  /** "Unsubscribe" */
  async clickUnsubscribeMeExcept(): Promise<void> {
    await buttonByLabel(this.page, 'Unsubscribe').click();
  }

  /** "Navigate Up" */
  async clickUp(): Promise<void> {
    await buttonByLabel(this.page, 'Navigate Up').click();
  }
}
