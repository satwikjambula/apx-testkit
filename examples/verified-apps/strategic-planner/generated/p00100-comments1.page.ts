/**
 * GENERATED by apx-testgen from pages/p00100-comments1.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class Comments1Page {
  static readonly alias = 'comments1';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, Comments1Page.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** richTextEditor, "Comment" */
  get comment(): ApexItem {
    return new ApexItem(this.page, 'P100_COMMENT');
  }

  /** hidden */
  get imageRefId(): ApexItem {
    return new ApexItem(this.page, 'P100_IMAGE_REF_ID');
  }

  /** checkbox, "This is a private comment" */
  get privateYn(): ApexItem {
    return new ApexItem(this.page, 'P100_PRIVATE_YN');
  }

  /** displayOnly, "Project" */
  get projectId(): ApexItem {
    return new ApexItem(this.page, 'P100_PROJECT_ID');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Post Comment" */
  async clickPostComment(): Promise<void> {
    await buttonByLabel(this.page, 'Post Comment').click();
  }

  /** "View Project" */
  async clickViewProject(): Promise<void> {
    await buttonByLabel(this.page, 'View Project').click();
  }
}
