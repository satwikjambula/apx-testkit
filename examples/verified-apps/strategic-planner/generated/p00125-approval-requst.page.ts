/**
 * GENERATED by apx-testgen from pages/p00125-approval-requst.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ApprovalRequstPage {
  static readonly alias = 'approval-requst';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ApprovalRequstPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get approvalType(): ApexItem {
    return new ApexItem(this.page, 'P125_APPROVAL_TYPE');
  }

  /** textarea, "Comments" */
  get comments(): ApexItem {
    return new ApexItem(this.page, 'P125_COMMENTS');
  }

  /** hidden */
  get projectApprovalChainId(): ApexItem {
    return new ApexItem(this.page, 'P125_PROJECT_APPROVAL_CHAIN_ID');
  }

  /** hidden */
  get projectApprovalId(): ApexItem {
    return new ApexItem(this.page, 'P125_PROJECT_APPROVAL_ID');
  }

  /** hidden */
  get wfTaskId(): ApexItem {
    return new ApexItem(this.page, 'P125_WF_TASK_ID');
  }

  /** "Approve" */
  async clickApprove(): Promise<void> {
    await buttonByLabel(this.page, 'Approve').click();
  }

  /** "Reject" */
  async clickReject(): Promise<void> {
    await buttonByLabel(this.page, 'Reject').click();
  }

  /** "Request Clarification" */
  async clickRequestClarification(): Promise<void> {
    await buttonByLabel(this.page, 'Request Clarification').click();
  }

  /** "Withdraw Request" */
  async clickWithdraw(): Promise<void> {
    await buttonByLabel(this.page, 'Withdraw Request').click();
  }
}
