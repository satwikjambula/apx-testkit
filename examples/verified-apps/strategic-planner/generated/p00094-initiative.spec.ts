/**
 * GENERATED by apx-testgen from pages/p00094-initiative.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00094-initiative.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: activities, additional-attriubtes, approval-type-content, approval-workflow-p94-approval-type-cnt, associated-releases, breadcrumb, comments, configuration, default-completeness-scale-p94-status-scale, default-tasks-p94-default-task-cnt, documents, focus-area-content, focus-areas, focus-areas-p94-focus-area-cnt, initiative-details, initiative-icon, links, menubar, nomenclature-initiative, nomenclature-projects, rds, status-scale-content, task-content, this-week, this-week-content
 * Region resolve-check emitted for 8 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): additional-attriubtes (classicReport), approval-workflow-p94-approval-type-cnt (staticContent), breadcrumb (breadcrumb), comments (classicReport), configuration (staticContent), default-completeness-scale-p94-status-scale (staticContent), default-tasks-p94-default-task-cnt (staticContent), focus-areas-p94-focus-area-cnt (staticContent), initiative-details (staticContent), initiative-icon (themeTemplateComponent/avatar), menubar (themeTemplateComponent/contentRow), nomenclature-initiative (classicReport), rds (regionDisplaySelector), status-scale-content (classicReport), task-content (classicReport), this-week (staticContent), this-week-content (themeTemplateComponent/contentRow).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle, login } from '@apx/testkit';
import { InitiativePage } from './p00094-initiative.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 94: Initiative [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new InitiativePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new InitiativePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&NOMENCLATURE_INITIATIVE.: &P94_INITIATIVE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (20 items, 12 hidden)', async ({ page }) => {
    const po = new InitiativePage(page);
    await po.goto();
    await expectItemsPresent(page, ['IFI', 'INN', 'P94_APPROVAL_TYPE_CNT', 'P94_AREA', 'P94_AREA_ID', 'P94_COMMENT', 'P94_DEFAULT_TASK_CNT', 'P94_FOCUS_AREA_CNT', 'P94_IMAGE_REF_ID', 'P94_INITIATIVE', 'P94_INITIATIVE_ID', 'P94_LINK', 'P94_LINK_NAME', 'P94_MIN_PC_FOR_STATUS', 'P94_PERMALINK_PREFIX', 'P94_PRIVATE_YN', 'P94_STATUS_SCALE', 'P94_STATUS_SCALE_LETTER', 'P94_THIS_WEEK_EXCLUDE', 'P94_THIS_WEEK_TYPE']);
  });

  test('apex.item round-trip on P94_LINK', async ({ page }) => {
    const po = new InitiativePage(page);
    await po.goto();
    await po.link.setValue('apx-testgen');
    expect(await po.link.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (11 buttons)', async ({ page }) => {
    const po = new InitiativePage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Add Activity', 'Associated New Type', 'Add Document', 'Add Focus Area', 'Add Link', 'Add Project', 'Select Different Completeness Scale', 'Edit Default Tasks', 'Post Comment', 'Navigate Up', 'Image Gallery']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (8 regions)', async ({ page }) => {
    const po = new InitiativePage(page);
    await po.goto();
    await expectRegionsResolve(page, ['activities', 'approval-type-content', 'associated-releases', 'DOCUMENTS', 'focus-area-content', 'focus-areas', 'links', 'nomenclature-projects']);
  });
});
