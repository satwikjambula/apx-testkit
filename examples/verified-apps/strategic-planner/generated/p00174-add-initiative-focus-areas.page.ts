/**
 * GENERATED by apx-testgen from pages/p00174-add-initiative-focus-areas.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class AddInitiativeFocusAreasPage {
  static readonly alias = 'add-initiative-focus-areas';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, AddInitiativeFocusAreasPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textField, "Description" */
  get description(): ApexItem {
    return new ApexItem(this.page, 'P174_DESCRIPTION');
  }

  /** textField, "Focus Area" */
  get focusArea(): ApexItem {
    return new ApexItem(this.page, 'P174_FOCUS_AREA');
  }

  /** selectOne, "Development Owner" */
  get ownerId(): ApexItem {
    return new ApexItem(this.page, 'P174_OWNER_ID');
  }

  /** "Add" */
  async clickAdd(): Promise<void> {
    await buttonByLabel(this.page, 'Add').click();
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Next" */
  async clickNext(): Promise<void> {
    await buttonByLabel(this.page, 'Next').click();
  }

  /** "Previous" */
  async clickPrevious(): Promise<void> {
    await buttonByLabel(this.page, 'Previous').click();
  }
}
