/**
 * GENERATED by apx-testgen from pages/p00003-project-details.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00003-project-details.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: activity, activity-content, activity-filters, additional-attriubtes, approvals, archived-nomenclature-project, breadcrumb, clarification-requested-for-review, comment-form, comments, comments-report, contributor-button-container, contributors, contributors-content, description, description-button-container, description-content, documents, duplicate-of, footer, links, menubar, milestone-button-container, milestone-content, milestones, project-details, rds, related, review-button-container, review-content, review-needed, reviews, task-content, tasks
 * Region resolve-check emitted for 7 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): activity (staticContent), activity-content (themeTemplateComponent/contentRow), activity-filters (staticContent), additional-attriubtes (classicReport), approvals (themeTemplateComponent/contentRow), archived-nomenclature-project (staticContent), breadcrumb (breadcrumb), clarification-requested-for-review (staticContent), comment-form (staticContent), comments (staticContent), comments-report (classicReport), contributor-button-container (staticContent), contributors (staticContent), description (staticContent), description-button-container (staticContent), description-content (dynamicContent), duplicate-of (staticContent), footer (staticContent), menubar (themeTemplateComponent/contentRow), milestone-button-container (staticContent), milestones (staticContent), project-details (classicReport), rds (regionDisplaySelector), review-button-container (staticContent), review-needed (staticContent), reviews (staticContent), tasks (staticContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle, login } from '@apx/testkit';
import { ProjectDetailsPage } from './p00003-project-details.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 3: Project Details [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&P3_PROJECT_NAME.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (22 items, 13 hidden)', async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    await po.goto();
    await expectItemsPresent(page, ['FI', 'P3_APPROVAL_TYPE', 'P3_COMMENT', 'P3_CONTRIBUTOR', 'P3_CONTRIBUTOR_ROLE', 'P3_DUP_PROJECT', 'P3_DUP_PROJECT_ID', 'P3_HAS_VOTED_CLASS', 'P3_IMAGE_REF_ID', 'P3_INCLUDE_FUTURE', 'P3_INCLUDE_PAST', 'P3_LINK', 'P3_LINK_NAME', 'P3_MORE_INFO_APPROVAL_TYPE', 'P3_MORE_INFO_PROJECT_APPROVAL_CHAIN_ID', 'P3_PERMALINK_PREFIX', 'P3_PRIVATE_YN', 'P3_PROJECT_APPROVAL_CHAIN_ID', 'P3_PROJECT_ID', 'P3_PROJECT_NAME', 'P3_RELATED_PROJECT_ID', 'PN']);
  });

  test('apex.item round-trip on P3_LINK', async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    await po.goto();
    await po.link.setValue('apx-testgen');
    expect(await po.link.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (23 buttons)', async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['About Contributor Roles', 'Add Activity', 'Add Contributor', 'Add Document', 'Add Link', 'Add Milestone', 'Add Related &NOMENCLATURE_PROJECT.', 'Add Review', 'Add Task', 'Edit', 'Review', 'Post Comment', 'Request Approval', 'Review Now', 'Restore', 'Not a Duplicate', 'Navigate Up', 'View Contributor Change History', 'View Description Change History', 'View Primary', 'Image Gallery', 'View Milestone Change History', 'View Review Change History']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (7 regions)', async ({ page }) => {
    const po = new ProjectDetailsPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['contributors-content', 'DOCUMENTS', 'LINKS', 'milestone-content', 'RELATED', 'review-content', 'task-content']);
  });
});
