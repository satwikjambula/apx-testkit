/**
 * GENERATED by apx-testgen from pages/p09999-login.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p09999-login.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: nomenclature-strategic-planner
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): nomenclature-strategic-planner (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { LoginPage } from './p09999-login.page.js';

test.describe('page 9999: Login Page', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new LoginPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new LoginPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&NOMENCLATURE_STRATEGIC_PLANNER. - Log In'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (3 items, 0 hidden)', async ({ page }) => {
    const po = new LoginPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P9999_PASSWORD', 'P9999_REMEMBER', 'P9999_USERNAME']);
  });

  test('apex.item round-trip on P9999_USERNAME', async ({ page }) => {
    const po = new LoginPage(page);
    await po.goto();
    await po.username.setValue('apx-testgen');
    expect(await po.username.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new LoginPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Sign In']);
  });
});
