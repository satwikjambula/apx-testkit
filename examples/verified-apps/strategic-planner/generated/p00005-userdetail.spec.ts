/**
 * GENERATED by apx-testgen from pages/p00005-userdetail.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00005-userdetail.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: activity, approvals, breadcrumb, button-container, buttons, groups, menubar, milestones, my-nomenclature-project-changes, nomenclature-projects, note-on-report, profile, profile-details, project-change-content, rds, reviews, tasks
 * Region resolve-check emitted for 3 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): activity (themeTemplateComponent/contentRow), breadcrumb (breadcrumb), button-container (staticContent), buttons (staticContent), menubar (themeTemplateComponent/contentRow), milestones (classicReport), my-nomenclature-project-changes (staticContent), note-on-report (dynamicContent), profile (staticContent), profile-details (classicReport), project-change-content (dynamicContent), rds (regionDisplaySelector), reviews (classicReport), tasks (classicReport).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle, login } from '@apx/testkit';
import { UserdetailPage } from './p00005-userdetail.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 5: UserDetail [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new UserdetailPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new UserdetailPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('&P5_FIRST_NAME. &P5_LAST_NAME.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (10 items, 5 hidden)', async ({ page }) => {
    const po = new UserdetailPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P5_EMAIL', 'P5_EXT_LINK', 'P5_FIRST_NAME', 'P5_ID', 'P5_INCLUDE_FUTURE', 'P5_INCLUDE_PAST', 'P5_LAST_NAME', 'P5_PHOTO', 'P5_PHOTO_DEFAULT', 'P5_PROJ_CHANGE_EXCLUDE_USER_YN']);
  });

  test('every labeled .apx button exists (6 buttons)', async ({ page }) => {
    const po = new UserdetailPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Add', 'Email to me', 'External Directory', 'Subscribe me', 'Unsubscribe', 'Navigate Up']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (3 regions)', async ({ page }) => {
    const po = new UserdetailPage(page);
    await po.goto();
    await expectRegionsResolve(page, ['approvals', 'groups', 'nomenclature-projects']);
  });
});
