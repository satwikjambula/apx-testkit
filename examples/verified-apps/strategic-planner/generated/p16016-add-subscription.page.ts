/**
 * GENERATED by apx-testgen from pages/p16016-add-subscription.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class AddSubscriptionPage {
  static readonly alias = 'add-subscription';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, AddSubscriptionPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Frequency", required */
  get frequency(): ApexItem {
    return new ApexItem(this.page, 'P16016_FREQUENCY');
  }

  /** selectList, "Notification", required */
  get notificationId(): ApexItem {
    return new ApexItem(this.page, 'P16016_NOTIFICATION_ID');
  }

  /** hidden */
  get releaseExceptionsId(): ApexItem {
    return new ApexItem(this.page, 'P16016_RELEASE_EXCEPTIONS_ID');
  }

  /** popupLov, "Release" */
  get releaseId(): ApexItem {
    return new ApexItem(this.page, 'P16016_RELEASE_ID');
  }

  /** popupLov, "Team Member", required */
  get teamMemberId(): ApexItem {
    return new ApexItem(this.page, 'P16016_TEAM_MEMBER_ID');
  }

  /** "Add Subscription" */
  async clickAdd(): Promise<void> {
    await buttonByLabel(this.page, 'Add Subscription').click();
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }
}
