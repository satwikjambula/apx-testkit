/**
 * GENERATED by apx-testgen from pages/p00117-release2.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00117-release2.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: add-comment, breadcrumb, comments, contributors, documents, exception-buttons, exceptions, exceptions-body, links, menubar, milestones, nomenclature-projects, rds, release, reviewers, timeline, timeline-buttons, timeline-contents
 * Region resolve-check emitted for 5 interactiveReport/cards/facetedSearch region(s) below (ADR-003 htmlDomId-resolved where set).
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): add-comment (staticContent), breadcrumb (breadcrumb), comments (classicReport), contributors (appTemplateComponent/contentRowSimple), exception-buttons (staticContent), exceptions (staticContent), exceptions-body (dynamicContent), menubar (themeTemplateComponent/contentRow), rds (regionDisplaySelector), release (classicReport), timeline (staticContent), timeline-buttons (staticContent), timeline-contents (dynamicContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, expectRegionsResolve, normalizeTitle, login } from '@apx/testkit';
import { Release2Page } from './p00117-release2.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 117: Release [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new Release2Page(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new Release2Page(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Release &P117_RELEASE.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (10 items, 5 hidden)', async ({ page }) => {
    const po = new Release2Page(page);
    await po.goto();
    await expectItemsPresent(page, ['P117_COMMENT', 'P117_CONTRIBUTOR_SEARCH', 'P117_EXCLUDE_COMPLETE_YN', 'P117_IMAGE_REF_ID', 'P117_INCLUDE_PAST_YN', 'P117_LINK', 'P117_LINK_NAME', 'P117_PRIVATE_YN', 'P117_RELEASE', 'P117_RELEASE_ID']);
  });

  test('apex.item round-trip on P117_CONTRIBUTOR_SEARCH', async ({ page }) => {
    const po = new Release2Page(page);
    await po.goto();
    await po.contributorSearch.setValue('apx-testgen');
    expect(await po.contributorSearch.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (18 buttons)', async ({ page }) => {
    const po = new Release2Page(page);
    await po.goto();
    await expectButtonsPresent(page, ['Add Document', 'Add Link', 'Add Milestone', 'Add &NOMENCLATURE_PROJECT.', 'Calendar View', 'Search', 'Email to me', 'Email to me', 'Exclude Completed', 'Exclude Past', 'Include Completed', 'Include Past', 'Post Comment', 'Subscribe me', 'Unsubscribe', 'Navigate Up', 'View All', 'Image Gallery']);
  });

  test('every interactiveReport/cards/facetedSearch region resolves (5 regions)', async ({ page }) => {
    const po = new Release2Page(page);
    await po.goto();
    await expectRegionsResolve(page, ['DOCUMENTS', 'links', 'MILESTONES', 'nomenclature-projects', 'reviewers']);
  });
});
