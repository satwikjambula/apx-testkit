/**
 * GENERATED by apx-testgen from pages/p00126-clarification-requested.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ClarificationRequestedPage {
  static readonly alias = 'clarification-requested';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ClarificationRequestedPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get approvalType(): ApexItem {
    return new ApexItem(this.page, 'P126_APPROVAL_TYPE');
  }

  /** textarea, "Response" */
  get comments(): ApexItem {
    return new ApexItem(this.page, 'P126_COMMENTS');
  }

  /** hidden */
  get projectApprovalChainId(): ApexItem {
    return new ApexItem(this.page, 'P126_PROJECT_APPROVAL_CHAIN_ID');
  }

  /** hidden */
  get projectApprovalId(): ApexItem {
    return new ApexItem(this.page, 'P126_PROJECT_APPROVAL_ID');
  }

  /** hidden */
  get wfTaskId(): ApexItem {
    return new ApexItem(this.page, 'P126_WF_TASK_ID');
  }

  /** "Submit" */
  async clickSubmit(): Promise<void> {
    await buttonByLabel(this.page, 'Submit').click();
  }

  /** "Withdraw Review" */
  async clickWithdraw(): Promise<void> {
    await buttonByLabel(this.page, 'Withdraw Review').click();
  }
}
