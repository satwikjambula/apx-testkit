/**
 * GENERATED by apx-testgen from pages/p04001-workflow-details.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class WorkflowDetailsPage {
  static readonly alias = 'workflow-details';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, WorkflowDetailsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get activityId(): ApexItem {
    return new ApexItem(this.page, 'P4001_ACTIVITY_ID');
  }

  /** hidden */
  get parentWorkflowId(): ApexItem {
    return new ApexItem(this.page, 'P4001_PARENT_WORKFLOW_ID');
  }

  /** hidden */
  get workflowId(): ApexItem {
    return new ApexItem(this.page, 'P4001_WORKFLOW_ID');
  }

  /** "Close" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Close').click();
  }

  /** "Close" */
  async clickCancelActivityAudit(): Promise<void> {
    await buttonByLabel(this.page, 'Close').click();
  }

  /** "Resume" */
  async clickResume(): Promise<void> {
    await buttonByLabel(this.page, 'Resume').click();
  }

  /** "Retry" */
  async clickRetry(): Promise<void> {
    await buttonByLabel(this.page, 'Retry').click();
  }

  /** "Suspend" */
  async clickSuspend(): Promise<void> {
    await buttonByLabel(this.page, 'Suspend').click();
  }

  /** "Terminate" */
  async clickTerminate(): Promise<void> {
    await buttonByLabel(this.page, 'Terminate').click();
  }

  /** "To Parent Workflow" */
  async clickToParentWorkflow(): Promise<void> {
    await buttonByLabel(this.page, 'To Parent Workflow').click();
  }
}
