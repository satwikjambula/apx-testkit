/**
 * GENERATED by apx-testgen from pages/p00073-user-quick-look.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class UserQuickLookPage {
  static readonly alias = 'user-quick-look';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, UserQuickLookPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get extLink(): ApexItem {
    return new ApexItem(this.page, 'P73_EXT_LINK');
  }

  /** displayImage, "Photo" */
  get photo(): ApexItem {
    return new ApexItem(this.page, 'P73_PHOTO');
  }

  /** displayImage, "Photo" */
  get photoPlaceholder(): ApexItem {
    return new ApexItem(this.page, 'P73_PHOTO_PLACEHOLDER');
  }

  /** hidden */
  get teamMemberId(): ApexItem {
    return new ApexItem(this.page, 'P73_TEAM_MEMBER_ID');
  }

  /** "Edit (Admin)" */
  async clickEditUser(): Promise<void> {
    await buttonByLabel(this.page, 'Edit (Admin)').click();
  }

  /** "External Directory" */
  async clickExternalLink(): Promise<void> {
    await buttonByLabel(this.page, 'External Directory').click();
  }

  /** "Manage My Profile" */
  async clickManageMyProfile(): Promise<void> {
    await buttonByLabel(this.page, 'Manage My Profile').click();
  }

  /** "View Details" */
  async clickViewDetails(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }
}
