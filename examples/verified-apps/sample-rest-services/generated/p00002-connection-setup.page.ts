/**
 * GENERATED by apx-testgen from pages/p00002-connection-setup.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ConnectionSetupPage {
  static readonly alias = 'connection-setup';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ConnectionSetupPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textField, "Base URL", required */
  get baseUrl(): ApexItem {
    return new ApexItem(this.page, 'P2_BASE_URL');
  }

  /** textField, "OData Endpoint URL" */
  get odataUrl(): ApexItem {
    return new ApexItem(this.page, 'P2_ODATA_URL');
  }

  /** textField, "Proxy  Server" */
  get proxy(): ApexItem {
    return new ApexItem(this.page, 'P2_PROXY');
  }

  /** textField, ""Schema Alias "", required */
  get schema(): ApexItem {
    return new ApexItem(this.page, 'P2_SCHEMA');
  }

  /** "Complete Setup" */
  async clickCompleteSetup(): Promise<void> {
    await buttonByLabel(this.page, 'Complete Setup').click();
  }
}
