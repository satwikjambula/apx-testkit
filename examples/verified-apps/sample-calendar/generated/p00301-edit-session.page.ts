/**
 * GENERATED by apx-testgen from pages/p00301-edit-session.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class EditSessionPage {
  static readonly alias = 'edit-session';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, EditSessionPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** datePicker, "End Date", required */
  get endDate(): ApexItem {
    return new ApexItem(this.page, 'P301_END_DATE');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P301_ID');
  }

  /** hidden */
  get rowVersionNumber(): ApexItem {
    return new ApexItem(this.page, 'P301_ROW_VERSION_NUMBER');
  }

  /** selectList, "Session Type", required */
  get sessionType(): ApexItem {
    return new ApexItem(this.page, 'P301_SESSION_TYPE');
  }

  /** textField, "Speaker" */
  get speaker(): ApexItem {
    return new ApexItem(this.page, 'P301_SPEAKER');
  }

  /** datePicker, "Start Date", required */
  get startDate(): ApexItem {
    return new ApexItem(this.page, 'P301_START_DATE');
  }

  /** radioGroup, "Status", required */
  get status(): ApexItem {
    return new ApexItem(this.page, 'P301_STATUS');
  }

  /** textField, "Title", required */
  get title(): ApexItem {
    return new ApexItem(this.page, 'P301_TITLE');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Create" */
  async clickCreate(): Promise<void> {
    await buttonByLabel(this.page, 'Create').click();
  }

  /** "Delete" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
