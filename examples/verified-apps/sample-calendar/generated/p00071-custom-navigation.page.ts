/**
 * GENERATED by apx-testgen from pages/p00071-custom-navigation.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class CustomNavigationPage {
  static readonly alias = 'custom-navigation';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, CustomNavigationPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** datePicker, "Goto date" */
  get gotodate(): ApexItem {
    return new ApexItem(this.page, 'P71_GOTODATE');
  }

  /** selectList, "Project" */
  get projects(): ApexItem {
    return new ApexItem(this.page, 'P71_PROJECTS');
  }

  /** selectList, "View Type" */
  get viewtype(): ApexItem {
    return new ApexItem(this.page, 'P71_VIEWTYPE');
  }

  /** "Next Calendar Page" */
  async clickNext(): Promise<void> {
    await buttonByLabel(this.page, 'Next Calendar Page').click();
  }

  /** "Previous Calendar Page" */
  async clickPrev(): Promise<void> {
    await buttonByLabel(this.page, 'Previous Calendar Page').click();
  }

  /** "Today" */
  async clickToday(): Promise<void> {
    await buttonByLabel(this.page, 'Today').click();
  }
}
