/**
 * GENERATED by apx-testgen from pages/p00074-schedule-builder.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ScheduleBuilderPage {
  static readonly alias = 'schedule-builder';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ScheduleBuilderPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** datePicker, "To" */
  get enddate(): ApexItem {
    return new ApexItem(this.page, 'P74_ENDDATE');
  }

  /** hidden */
  get mySessionId(): ApexItem {
    return new ApexItem(this.page, 'P74_MY_SESSION_ID');
  }

  /** datePicker, "From" */
  get startdate(): ApexItem {
    return new ApexItem(this.page, 'P74_STARTDATE');
  }

  /** textField, "Title" */
  get title(): ApexItem {
    return new ApexItem(this.page, 'P74_TITLE');
  }

  /** "Clear Schedule" */
  async clickClear(): Promise<void> {
    await buttonByLabel(this.page, 'Clear Schedule').click();
  }

  /** "Save to Database" */
  async clickSaveInDatabase(): Promise<void> {
    await buttonByLabel(this.page, 'Save to Database').click();
  }

  /** "Show in Calendar" */
  async clickShowInCalendar(): Promise<void> {
    await buttonByLabel(this.page, 'Show in Calendar').click();
  }
}
