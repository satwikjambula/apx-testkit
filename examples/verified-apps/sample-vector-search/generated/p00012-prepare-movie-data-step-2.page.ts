/**
 * GENERATED by apx-testgen from pages/p00012-prepare-movie-data-step-2.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class PrepareMovieDataStep2Page {
  static readonly alias = 'prepare-movie-data-step-2';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, PrepareMovieDataStep2Page.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textarea, "AI Generated Data" */
  get aiOutput(): ApexItem {
    return new ApexItem(this.page, 'P12_AI_OUTPUT');
  }

  /** hidden */
  get sofar(): ApexItem {
    return new ApexItem(this.page, 'P12_SOFAR');
  }

  /** hidden */
  get totalwork(): ApexItem {
    return new ApexItem(this.page, 'P12_TOTALWORK');
  }

  /** "Back" */
  async clickPrev(): Promise<void> {
    await buttonByLabel(this.page, 'Back').click();
  }

  /** "Upload Data" */
  async clickUpload(): Promise<void> {
    await buttonByLabel(this.page, 'Upload Data').click();
  }
}
