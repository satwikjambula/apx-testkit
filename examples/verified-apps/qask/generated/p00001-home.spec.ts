/**
 * GENERATED by apx-testgen from pages/p00001-home.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00001-home.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: links, links-2, welcome-to-qask
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): links (staticContent), links-2 (staticContent), welcome-to-qask (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { HomePage } from './p00001-home.page.js';

test.describe('page 1: Home', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new HomePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new HomePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('qAsk'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (1 items, 0 hidden)', async ({ page }) => {
    const po = new HomePage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1_CODE']);
  });

  test('apex.item round-trip on P1_CODE', async ({ page }) => {
    const po = new HomePage(page);
    await po.goto();
    await po.code.setValue('apx-testgen');
    expect(await po.code.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (3 buttons)', async ({ page }) => {
    const po = new HomePage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Join', 'Host a Q&A Session', 'Sign In']);
  });
});
