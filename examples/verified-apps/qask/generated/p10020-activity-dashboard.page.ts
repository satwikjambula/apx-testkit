/**
 * GENERATED by apx-testgen from pages/p10020-activity-dashboard.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ActivityDashboardPage {
  static readonly alias = 'activity-dashboard';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ActivityDashboardPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Timeframe" */
  get timeframe(): ApexItem {
    return new ApexItem(this.page, 'P10020_TIMEFRAME');
  }

  /** "Up" */
  async clickUp(): Promise<void> {
    await buttonByLabel(this.page, 'Up').click();
  }

  /** "View Details" */
  async clickViewActivityByUser(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }

  /** "View Details" */
  async clickViewActivityByUser2(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }

  /** "View Details" */
  async clickViewActivityDetails(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }

  /** "View Details" */
  async clickViewRecentErrors(): Promise<void> {
    await buttonByLabel(this.page, 'View Details').click();
  }
}
