/**
 * GENERATED by apx-testgen from pages/p00050-host-your-own.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00050-host-your-own.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: buttons, host-your-own-sessions
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): buttons (staticContent), host-your-own-sessions (form).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { HostYourOwnPage } from './p00050-host-your-own.page.js';

test.describe('page 50: Host Your Own Sessions', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new HostYourOwnPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new HostYourOwnPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Host Your Own Sessions'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (5 items, 2 hidden)', async ({ page }) => {
    const po = new HostYourOwnPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P50_ACCEPTED_TERMS_ID', 'P50_EMAIL', 'P50_ID', 'P50_JUSTIFICATION', 'P50_TERMS']);
  });

  test('apex.item round-trip on P50_EMAIL', async ({ page }) => {
    const po = new HostYourOwnPage(page);
    await po.goto();
    await po.email.setValue('apx-testgen');
    expect(await po.email.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new HostYourOwnPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Submit']);
  });
});
