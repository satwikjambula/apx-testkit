/**
 * GENERATED by apx-testgen from pages/p00120-session-summary.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class SessionSummaryPage {
  static readonly alias = 'session-summary';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, SessionSummaryPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get sessionCode(): ApexItem {
    return new ApexItem(this.page, 'P120_SESSION_CODE');
  }

  /** hidden */
  get sessionId(): ApexItem {
    return new ApexItem(this.page, 'P120_SESSION_ID');
  }

  /** hidden */
  get sessionName(): ApexItem {
    return new ApexItem(this.page, 'P120_SESSION_NAME');
  }

  /** "Actions Menu" */
  async clickActionsMenu(): Promise<void> {
    await buttonByLabel(this.page, 'Actions Menu').click();
  }

  /** "Ask Question" */
  async clickAskQuestion(): Promise<void> {
    await buttonByLabel(this.page, 'Ask Question').click();
  }

  /** "Close Session" */
  async clickCloseSession(): Promise<void> {
    await buttonByLabel(this.page, 'Close Session').click();
  }

  /** "Start Session" */
  async clickStartSession(): Promise<void> {
    await buttonByLabel(this.page, 'Start Session').click();
  }

  /** "Navigate Up" */
  async clickUp(): Promise<void> {
    await buttonByLabel(this.page, 'Navigate Up').click();
  }
}
