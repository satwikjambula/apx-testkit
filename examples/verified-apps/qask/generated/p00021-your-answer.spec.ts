/**
 * GENERATED by apx-testgen from pages/p00021-your-answer.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00021-your-answer.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: container, last-question-closed, next-question-available, next-question-unavailable, p20-question, session-closed, session-session-code
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): container (staticContent), last-question-closed (staticContent), next-question-available (staticContent), next-question-unavailable (staticContent), p20-question (plSqlDynamicContent), session-closed (staticContent), session-session-code (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { YourAnswerPage } from './p00021-your-answer.page.js';

test.describe('page 21: Your Answer', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new YourAnswerPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new YourAnswerPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Your Answer'));
  });

  test('every labeled .apx button exists (3 buttons)', async ({ page }) => {
    const po = new YourAnswerPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Join Another Session', 'Next Question', 'Refresh']);
  });
});
