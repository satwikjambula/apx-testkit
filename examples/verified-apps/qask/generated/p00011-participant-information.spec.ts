/**
 * GENERATED by apx-testgen from pages/p00011-participant-information.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00011-participant-information.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: welcome-to-qask
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): welcome-to-qask (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { ParticipantInformationPage } from './p00011-participant-information.page.js';

test.describe('page 11: Participant Information', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ParticipantInformationPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ParticipantInformationPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Participant Information'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 0 hidden)', async ({ page }) => {
    const po = new ParticipantInformationPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P11_EMAIL', 'P11_NAME']);
  });

  test('apex.item round-trip on P11_EMAIL', async ({ page }) => {
    const po = new ParticipantInformationPage(page);
    await po.goto();
    await po.email.setValue('apx-testgen');
    expect(await po.email.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new ParticipantInformationPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Join']);
  });
});
