/**
 * GENERATED by apx-testgen from pages/p00020-question.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00020-question.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: answered, p20-question, session-session-code
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): answered (staticContent), p20-question (staticContent), session-session-code (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { QuestionPage } from './p00020-question.page.js';

test.describe('page 20: Question', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new QuestionPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new QuestionPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Question'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (10 items, 4 hidden)', async ({ page }) => {
    const po = new QuestionPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P20_ANSWER_FREEFORM', 'P20_ANSWER_ID', 'P20_ANSWER_TYPE', 'P20_ASK_FOR_COMMENTS_YN', 'P20_COMMENTS', 'P20_MULTI_ANSWER', 'P20_QUESTION', 'P20_QUESTION_EXPLANATION', 'P20_QUESTION_FILE', 'P20_QUESTION_NUMBER']);
  });

  test('apex.item round-trip on P20_ANSWER_FREEFORM', async ({ page }) => {
    const po = new QuestionPage(page);
    await po.goto();
    await po.answerFreeform.setValue('apx-testgen');
    expect(await po.answerFreeform.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new QuestionPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Continue', 'Submit Answer']);
  });
});
