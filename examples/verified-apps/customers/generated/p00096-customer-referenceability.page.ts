/**
 * GENERATED by apx-testgen from pages/p00096-customer-referenceability.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class CustomerReferenceabilityPage {
  static readonly alias = 'customer-referenceability';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, CustomerReferenceabilityPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P96_ID');
  }

  /** selectList, "Publicly Referenceable" */
  get referencable(): ApexItem {
    return new ApexItem(this.page, 'P96_REFERENCABLE');
  }

  /** selectList, "Reference Phase" */
  get referencePhaseId(): ApexItem {
    return new ApexItem(this.page, 'P96_REFERENCE_PHASE_ID');
  }

  /** checkboxGroup, "Reference Types" */
  get referenceTypes(): ApexItem {
    return new ApexItem(this.page, 'P96_REFERENCE_TYPES');
  }

  /** textField, "Recruitment Owner" */
  get refRecruitmentOwner(): ApexItem {
    return new ApexItem(this.page, 'P96_REF_RECRUITMENT_OWNER');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
