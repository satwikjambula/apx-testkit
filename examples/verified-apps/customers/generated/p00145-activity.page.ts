/**
 * GENERATED by apx-testgen from pages/p00145-activity.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ActivityPage {
  static readonly alias = 'activity';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ActivityPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get activity(): ApexItem {
    return new ApexItem(this.page, 'P145_ACTIVITY');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P145_ID');
  }

  /** "Edit Activity" */
  async clickEdit(): Promise<void> {
    await buttonByLabel(this.page, 'Edit Activity').click();
  }

  /** "Add Attachment" */
  async clickPopattachment(): Promise<void> {
    await buttonByLabel(this.page, 'Add Attachment').click();
  }

  /** "Reload" */
  async clickReload(): Promise<void> {
    await buttonByLabel(this.page, 'Reload').click();
  }

  /** "View Attachments" */
  async clickViewAttachments(): Promise<void> {
    await buttonByLabel(this.page, 'View Attachments').click();
  }
}
