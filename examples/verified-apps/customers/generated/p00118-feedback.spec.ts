/**
 * GENERATED by apx-testgen from pages/p00118-feedback.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00118-feedback.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: buttons, feedback
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): buttons (staticContent), feedback (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { FeedbackPage } from './p00118-feedback.page.js';

test.describe('page 118: Feedback', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new FeedbackPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new FeedbackPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Feedback'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (4 items, 1 hidden)', async ({ page }) => {
    const po = new FeedbackPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P118_APPLICATION_ID', 'P118_FEEDBACK', 'P118_FEEDBACK_TYPE', 'P118_PAGE_ID']);
  });

  test('apex.item round-trip on P118_FEEDBACK', async ({ page }) => {
    const po = new FeedbackPage(page);
    await po.goto();
    await po.feedback.setValue('apx-testgen');
    expect(await po.feedback.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (2 buttons)', async ({ page }) => {
    const po = new FeedbackPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Cancel', 'Submit Feedback']);
  });
});
