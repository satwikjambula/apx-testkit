/**
 * GENERATED by apx-testgen from pages/p00130-access-control-configuration2.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class AccessControlConfiguration2Page {
  static readonly alias = 'access-control-configuration2';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, AccessControlConfiguration2Page.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** radioGroup, "Treat authenticated users not in ACL as" */
  get accessControlScope(): ApexItem {
    return new ApexItem(this.page, 'P130_ACCESS_CONTROL_SCOPE');
  }

  /** switch, "Any authenticated user may access this application", required */
  get acEnabled(): ApexItem {
    return new ApexItem(this.page, 'P130_AC_ENABLED');
  }

  /** hidden */
  get hiddenAcEnabled(): ApexItem {
    return new ApexItem(this.page, 'P130_HIDDEN_AC_ENABLED');
  }

  /** switch, "Use Email for Usernames", required */
  get usernameFormat(): ApexItem {
    return new ApexItem(this.page, 'P130_USERNAME_FORMAT');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
