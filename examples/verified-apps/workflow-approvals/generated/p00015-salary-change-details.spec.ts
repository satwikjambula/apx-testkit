/**
 * GENERATED by apx-testgen from pages/p00015-salary-change-details.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00015-salary-change-details.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: about-this-page, add-comment, buttons, cancel-task, comments, delegate, details, due, edit-parameter, history, invite-participant, priority, remove-participant, request-information, salary-history, salary-increment, subject, submit-information
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * 1 chart region(s) SKIPPED -- no htmlDomId set, runtime id genuinely unconstructible from static data (ADR-003 layer 3): salary-history.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): about-this-page (staticContent), add-comment (staticContent), buttons (staticContent), cancel-task (staticContent), comments (themeTemplateComponent/comments), delegate (staticContent), details (themeTemplateComponent/contentRow), due (staticContent), edit-parameter (staticContent), history (themeTemplateComponent/comments), invite-participant (staticContent), priority (staticContent), remove-participant (staticContent), request-information (staticContent), salary-increment (classicReport), subject (themeTemplateComponent/contentRow), submit-information (staticContent).
 * This page is not authentication:public. Tests log in via @apx/testkit's
 * login() in a beforeEach, gated on APX_LOGIN_TEST_USERNAME/
 * APX_LOGIN_TEST_PASSWORD -- skips cleanly at runtime if either is unset,
 * rather than being permanently skipped at file-load time. login() assumes
 * this app's DEFAULT APEX authentication scheme, with a standard
 * P101_USERNAME/P101_PASSWORD login page reachable at <app-base>/login
 * (confirmed live against a real APEX app -- see
 * spike/tests/auth-login-verify.spec.ts). Apps using a CUSTOM
 * authentication scheme will get a clear, specific error straight from
 * login() itself (e.g. "P101_USERNAME not found") instead of a silent
 * hang -- that is the correct, intended failure mode for those apps, not a
 * bug to work around here.
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle, login } from '@apx/testkit';
import { SalaryChangeDetailsPage } from './p00015-salary-change-details.page.js';
import { APP_BASE } from '../playwright.config.js';

test.describe('page 15: Salary Change Details [requires auth]', () => {
  test.beforeEach(async ({ page }) => {
    const username = process.env.APX_LOGIN_TEST_USERNAME;
    const password = process.env.APX_LOGIN_TEST_PASSWORD;
    test.skip(
      !username || !password,
      'Set APX_LOGIN_TEST_USERNAME and APX_LOGIN_TEST_PASSWORD to run tests against this authenticated page.',
    );
    await page.goto(`${APP_BASE}/login`, { waitUntil: 'domcontentloaded' });
    await login(page, { username: username!, password: password! });
  });
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new SalaryChangeDetailsPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new SalaryChangeDetailsPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Salary Change Details'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (17 items, 4 hidden)', async ({ page }) => {
    const po = new SalaryChangeDetailsPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P15_ALL_COMMENTS', 'P15_ALL_HISTORY', 'P15_CAN_UPDATE_PARAMS', 'P15_COMMENT_TEXT', 'P15_IS_REQUIRED', 'P15_NEW_DUE_DATE', 'P15_NEW_OWNER', 'P15_NEW_POTENTIAL_OWNER', 'P15_NEW_PRIORITY', 'P15_NEW_VALUE', 'P15_PARAM_LABEL', 'P15_PARAM_STATIC_ID', 'P15_PARAM_VALUE', 'P15_POTENTIAL_OWNER', 'P15_REQUEST_INFO_TEXT', 'P15_SUBMIT_INFO_TEXT', 'P15_TASK_ID']);
  });

  test('apex.item round-trip on P15_COMMENT_TEXT', async ({ page }) => {
    const po = new SalaryChangeDetailsPage(page);
    await po.goto();
    await po.commentText.setValue('apx-testgen');
    expect(await po.commentText.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (26 buttons)', async ({ page }) => {
    const po = new SalaryChangeDetailsPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Add Comment', 'Approve', 'Cancel', 'Cancel Task', 'Claim Task', 'Close', 'Close', 'Close', 'Close', 'Close', 'Close', 'Close', 'Close', 'Close', 'Close', 'Delegate', 'Invite Participant', 'Add Comment', 'Submit Information', 'Reject', 'Remove Participant', 'Request Information', 'Change Due Date', 'Change Priority', 'Submit Information', 'Apply Changes']);
  });
});
