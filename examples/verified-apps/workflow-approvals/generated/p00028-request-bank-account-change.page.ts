/**
 * GENERATED by apx-testgen from pages/p00028-request-bank-account-change.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class RequestBankAccountChangePage {
  static readonly alias = 'request-bank-account-change';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, RequestBankAccountChangePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** displayOnly, "Current Bank Account" */
  get bankAccount(): ApexItem {
    return new ApexItem(this.page, 'P28_BANK_ACCOUNT');
  }

  /** hidden */
  get empno(): ApexItem {
    return new ApexItem(this.page, 'P28_EMPNO');
  }

  /** displayOnly, "Ename" */
  get ename(): ApexItem {
    return new ApexItem(this.page, 'P28_ENAME');
  }

  /** textField, "New Bank Account", required */
  get newBankAccount(): ApexItem {
    return new ApexItem(this.page, 'P28_NEW_BANK_ACCOUNT');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Submit" */
  async clickSubmitForApproval(): Promise<void> {
    await buttonByLabel(this.page, 'Submit').click();
  }
}
