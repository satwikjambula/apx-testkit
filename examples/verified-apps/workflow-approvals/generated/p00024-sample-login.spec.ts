/**
 * GENERATED by apx-testgen from pages/p00024-sample-login.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00024-sample-login.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: demo-purposes-only-login, info, info-2, operations-team, right-side, team-members
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Region types NOT covered by an auto-generated assertion (no verified DOM convention, or a runtime id genuinely unconstructible from static data -- see docs/grammar-assumptions.md "Still open" and ADR-003): demo-purposes-only-login (tree), info (staticContent), info-2 (staticContent), operations-team (staticContent), right-side (staticContent), team-members (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { SampleLoginPage } from './p00024-sample-login.page.js';

test.describe('page 24: Sample Login', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new SampleLoginPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new SampleLoginPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Sample Login'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 2 hidden)', async ({ page }) => {
    const po = new SampleLoginPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P24_SELECTED_EMPNO', 'P24_SELECTED_USERNAME']);
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new SampleLoginPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Login']);
  });
});
