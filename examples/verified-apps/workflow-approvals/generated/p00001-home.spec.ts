/**
 * GENERATED by apx-testgen from pages/p00001-home.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00001-home.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: about-this-app, app-name, create-job-privilege-required, oracle-apex-communities-and-resources
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): about-this-app (staticContent), app-name (staticContent), create-job-privilege-required (staticContent), oracle-apex-communities-and-resources (plugin/sampleAppsFooter).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { HomePage } from './p00001-home.page.js';

test.describe('page 1: Home', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new HomePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new HomePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Home'));
  });
});
