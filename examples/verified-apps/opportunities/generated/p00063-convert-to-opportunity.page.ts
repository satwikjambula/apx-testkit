/**
 * GENERATED by apx-testgen from pages/p00063-convert-to-opportunity.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ConvertToOpportunityPage {
  static readonly alias = 'convert-to-opportunity';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ConvertToOpportunityPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Account", required */
  get account(): ApexItem {
    return new ApexItem(this.page, 'P63_ACCOUNT');
  }

  /** numberField, "Amount", required */
  get amount(): ApexItem {
    return new ApexItem(this.page, 'P63_AMOUNT');
  }

  /** datePicker, "Close Date", required */
  get closeDate(): ApexItem {
    return new ApexItem(this.page, 'P63_CLOSE_DATE');
  }

  /** hidden */
  get leadId(): ApexItem {
    return new ApexItem(this.page, 'P63_LEAD_ID');
  }

  /** textField, "Opportunity", required */
  get opportunity(): ApexItem {
    return new ApexItem(this.page, 'P63_OPPORTUNITY');
  }

  /** selectList, "Primary &REP_TITLE.", required */
  get salesRep(): ApexItem {
    return new ApexItem(this.page, 'P63_SALES_REP');
  }

  /** selectList, "Status" */
  get status(): ApexItem {
    return new ApexItem(this.page, 'P63_STATUS');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Convert to Opportunity" */
  async clickConvertToOpportunity(): Promise<void> {
    await buttonByLabel(this.page, 'Convert to Opportunity').click();
  }
}
