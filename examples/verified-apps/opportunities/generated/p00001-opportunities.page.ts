/**
 * GENERATED by apx-testgen from pages/p00001-opportunities.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class OpportunitiesPage {
  static readonly alias = 'opportunities';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, OpportunitiesPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Account" */
  get accountId(): ApexItem {
    return new ApexItem(this.page, 'P1_ACCOUNT_ID');
  }

  /** radioGroup, "Display" */
  get displayAs(): ApexItem {
    return new ApexItem(this.page, 'P1_DISPLAY_AS');
  }

  /** selectList, "Quarter" */
  get quarter(): ApexItem {
    return new ApexItem(this.page, 'P1_QUARTER');
  }

  /** textField, "Search" */
  get search(): ApexItem {
    return new ApexItem(this.page, 'P1_SEARCH');
  }

  /** selectList, "Show" */
  get show(): ApexItem {
    return new ApexItem(this.page, 'P1_SHOW');
  }

  /** selectList, "Sort" */
  get sort(): ApexItem {
    return new ApexItem(this.page, 'P1_SORT');
  }

  /** selectList, "Account Territory" */
  get territoryId(): ApexItem {
    return new ApexItem(this.page, 'P1_TERRITORY_ID');
  }

  /** "Create Opportunity" */
  async clickCreateDeal(): Promise<void> {
    await buttonByLabel(this.page, 'Create Opportunity').click();
  }

  /** "Reset" */
  async clickResetReport(): Promise<void> {
    await buttonByLabel(this.page, 'Reset').click();
  }

  /** "Reset" */
  async clickResetReport2(): Promise<void> {
    await buttonByLabel(this.page, 'Reset').click();
  }

  /** "Upload Data" */
  async clickUpload(): Promise<void> {
    await buttonByLabel(this.page, 'Upload Data').click();
  }
}
