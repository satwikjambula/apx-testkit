/**
 * GENERATED by apx-testgen from pages/p00213-data-load-results2.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class DataLoadResults2Page {
  static readonly alias = 'data-load-results2';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, DataLoadResults2Page.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** displayOnly, "Failed Row(s):" */
  get errorCount(): ApexItem {
    return new ApexItem(this.page, 'P213_ERROR_COUNT');
  }

  /** displayOnly, "Inserted Row(s):" */
  get insertCount(): ApexItem {
    return new ApexItem(this.page, 'P213_INSERT_COUNT');
  }

  /** displayOnly, "Updated Row(s):" */
  get updateCount(): ApexItem {
    return new ApexItem(this.page, 'P213_UPDATE_COUNT');
  }

  /** "Finish" */
  async clickFinish(): Promise<void> {
    await buttonByLabel(this.page, 'Finish').click();
  }
}
