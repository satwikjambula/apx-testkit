/**
 * GENERATED by apx-testgen from pages/p00120-support-amount.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class SupportAmountPage {
  static readonly alias = 'support-amount';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, SupportAmountPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Support Amount Type", required */
  get amountTypeId(): ApexItem {
    return new ApexItem(this.page, 'P120_AMOUNT_TYPE_ID');
  }

  /** textarea, "Description" */
  get description(): ApexItem {
    return new ApexItem(this.page, 'P120_DESCRIPTION');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P120_ID');
  }

  /** textField, "Name" */
  get name(): ApexItem {
    return new ApexItem(this.page, 'P120_NAME');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Add Support Amount" */
  async clickCreate(): Promise<void> {
    await buttonByLabel(this.page, 'Add Support Amount').click();
  }

  /** "Delete" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
