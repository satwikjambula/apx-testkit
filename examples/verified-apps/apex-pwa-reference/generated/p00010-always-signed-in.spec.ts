/**
 * GENERATED by apx-testgen from pages/p00010-always-signed-in.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00010-always-signed-in.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: apex-admin-instance, apex-authentication-login, breadcrumb, example, image, image-2, image-3, increased-engagement, instructions, login-page-default, login-page-with-pwa, manage-instance, overview, region-display-selector, secured-by-default, using-the-api
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): apex-admin-instance (staticContent), apex-authentication-login (staticContent), breadcrumb (breadcrumb), example (staticContent), image (staticContent), image-2 (staticContent), image-3 (staticContent), increased-engagement (staticContent), instructions (staticContent), login-page-default (staticContent), login-page-with-pwa (staticContent), manage-instance (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), secured-by-default (staticContent), using-the-api (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { AlwaysSignedInPage } from './p00010-always-signed-in.page.js';

test.describe('page 10: Always Signed In', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new AlwaysSignedInPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new AlwaysSignedInPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Always Signed In - &APP_NAME.'));
  });
});
