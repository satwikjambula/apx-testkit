/**
 * GENERATED by apx-testgen from pages/p00005-service-worker.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00005-service-worker.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, default, file-url, hooks, overview, region-display-selector, service-worker-interface
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), default (staticContent), file-url (staticContent), hooks (staticContent), overview (staticContent), region-display-selector (regionDisplaySelector), service-worker-interface (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { ServiceWorkerPage } from './p00005-service-worker.page.js';

test.describe('page 5: Service Worker', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new ServiceWorkerPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new ServiceWorkerPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Service Worker - &APP_NAME.'));
  });
});
