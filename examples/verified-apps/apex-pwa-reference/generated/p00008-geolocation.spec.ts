/**
 * GENERATED by apx-testgen from pages/p00008-geolocation.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00008-geolocation.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, compatibility, data-type, demo, edit-dynamic-action, examples, geojson, geojson-code, get-current-position, image, image-2, image-3, instructions, ios-requesting-permission, ios-returning-geolocation, item-container, javascript-function, javascript-function-code, latitude-and-longitude, latitude-and-longitude-code, region-display-selector
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), compatibility (staticContent), data-type (staticContent), demo (staticContent), edit-dynamic-action (staticContent), examples (staticContent), geojson (staticContent), geojson-code (staticContent), get-current-position (staticContent), image (staticContent), image-2 (staticContent), image-3 (staticContent), instructions (staticContent), ios-requesting-permission (staticContent), ios-returning-geolocation (staticContent), item-container (staticContent), javascript-function (staticContent), javascript-function-code (staticContent), latitude-and-longitude (staticContent), latitude-and-longitude-code (staticContent), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { GeolocationPage } from './p00008-geolocation.page.js';

test.describe('page 8: Geolocation', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new GeolocationPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new GeolocationPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Geolocation - &APP_NAME.'));
  });

  test('every .apx pageItem exists in DOM/apex.item registry (2 items, 0 hidden)', async ({ page }) => {
    const po = new GeolocationPage(page);
    await po.goto();
    await expectItemsPresent(page, ['P1_LATITUDE', 'P1_LONGITUDE']);
  });

  test('apex.item round-trip on P1_LATITUDE', async ({ page }) => {
    const po = new GeolocationPage(page);
    await po.goto();
    await po.p1Latitude.setValue('apx-testgen');
    expect(await po.p1Latitude.getValue()).toBe('apx-testgen');
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new GeolocationPage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Geolocate']);
  });
});
