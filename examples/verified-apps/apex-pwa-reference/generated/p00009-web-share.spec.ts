/**
 * GENERATED by apx-testgen from pages/p00009-web-share.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00009-web-share.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: breadcrumb, compatibility, demo, edit-dynamic-action, edit-meta-tag, examples, image, image-2, image-3, image-4, instructions, instructions-2, instructions-3, instructions-4, meta-tags, region-display-selector, sharing-an-apex-page-on-ios, sharing-an-apex-page-on-safari-desktop, sharing-apex-content
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): breadcrumb (breadcrumb), compatibility (staticContent), demo (staticContent), edit-dynamic-action (staticContent), edit-meta-tag (staticContent), examples (staticContent), image (staticContent), image-2 (staticContent), image-3 (staticContent), image-4 (staticContent), instructions (staticContent), instructions-2 (staticContent), instructions-3 (staticContent), instructions-4 (staticContent), meta-tags (staticContent), region-display-selector (regionDisplaySelector), sharing-an-apex-page-on-ios (staticContent), sharing-an-apex-page-on-safari-desktop (staticContent), sharing-apex-content (staticContent).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, expectButtonsPresent, normalizeTitle } from '@apx/testkit';
import { WebSharePage } from './p00009-web-share.page.js';

test.describe('page 9: Web Share', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new WebSharePage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new WebSharePage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Web Share - &APP_NAME.'));
  });

  test('every labeled .apx button exists (1 button)', async ({ page }) => {
    const po = new WebSharePage(page);
    await po.goto();
    await expectButtonsPresent(page, ['Share']);
  });
});
