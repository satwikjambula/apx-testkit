/**
 * GENERATED by apx-testgen from pages/p00004-app-icon.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00004-app-icon.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: app-icon, breadcrumb, compatibility, examples, image, image-2, image-3, image-4, instructions, instructions-2, instructions-3, instructions-4, managing-app-icon, managing-shortcuts, overview, pwa-shortcuts, pwa-shortcuts-on-android, pwa-shortcuts-on-macos, region-display-selector
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): app-icon (staticContent), breadcrumb (breadcrumb), compatibility (staticContent), examples (staticContent), image (staticContent), image-2 (staticContent), image-3 (staticContent), image-4 (staticContent), instructions (staticContent), instructions-2 (staticContent), instructions-3 (staticContent), instructions-4 (staticContent), managing-app-icon (staticContent), managing-shortcuts (staticContent), overview (staticContent), pwa-shortcuts (staticContent), pwa-shortcuts-on-android (staticContent), pwa-shortcuts-on-macos (staticContent), region-display-selector (regionDisplaySelector).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { AppIconPage } from './p00004-app-icon.page.js';

test.describe('page 4: App Icon', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new AppIconPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new AppIconPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('App Icon - &APP_NAME.'));
  });
});
