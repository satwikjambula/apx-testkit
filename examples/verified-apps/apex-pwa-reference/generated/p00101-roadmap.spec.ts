/**
 * GENERATED by apx-testgen from pages/p00101-roadmap.apx — DO NOT EDIT.
 * Regenerate after any .apx change; review this file's diff alongside the .apx diff.
 * Assertions limited to runtime-verified contracts (V1-V6), implemented once in
 * @apx/testkit — see that package before touching any of the helpers used below.
 * Navigation and item access go through the generated page object
 * (./p00101-roadmap.page.js), not raw testkit calls, so both stay in sync.
 * Regions present in metadata: apex, apex-2, apex-3, apex-4, apex-5, breadcrumb, region-display-selector, roadmap
 * No interactiveReport/cards/facetedSearch regions on this page -- no region resolve-check to emit.
 * Other region types NOT covered by an auto-generated assertion (no verified DOM convention -- see docs/grammar-assumptions.md "Still open"): apex (classicReport), apex-2 (classicReport), apex-3 (classicReport), apex-4 (classicReport), apex-5 (classicReport), breadcrumb (breadcrumb), region-display-selector (regionDisplaySelector), roadmap (classicReport).
 */
import { expect, test } from '@playwright/test';
import { expectItemsPresent, normalizeTitle } from '@apx/testkit';
import { RoadmapPage } from './p00101-roadmap.page.js';

test.describe('page 101: Roadmap', () => {
  test('loads via alias URL with clean console' , async ({ page }) => {
    const po = new RoadmapPage(page);
    const errors = await po.goto();
    await page.waitForTimeout(1000);
    expect(errors).toEqual([]);
  });

  test('title matches metadata (normalized)', async ({ page }) => {
    const po = new RoadmapPage(page);
    await po.goto();
    expect(normalizeTitle(await page.title())).toBe(normalizeTitle('Roadmap'));
  });
});
