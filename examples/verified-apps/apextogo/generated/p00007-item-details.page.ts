/**
 * GENERATED by apx-testgen from pages/p00007-item-details.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class ItemDetailsPage {
  static readonly alias = 'item-details';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, ItemDetailsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get itemDescription(): ApexItem {
    return new ApexItem(this.page, 'P7_ITEM_DESCRIPTION');
  }

  /** hidden */
  get itemId(): ApexItem {
    return new ApexItem(this.page, 'P7_ITEM_ID');
  }

  /** hidden */
  get itemName(): ApexItem {
    return new ApexItem(this.page, 'P7_ITEM_NAME');
  }

  /** textarea, "Add Note" */
  get note(): ApexItem {
    return new ApexItem(this.page, 'P7_NOTE');
  }

  /** numberField, "Quantity" */
  get quantity(): ApexItem {
    return new ApexItem(this.page, 'P7_QUANTITY');
  }

  /** hidden */
  get shoppingCartItems(): ApexItem {
    return new ApexItem(this.page, 'P7_SHOPPING_CART_ITEMS');
  }

  /** checkbox, "<span class="fa fa-cutlery" aria-hidden="true"></span> Request utensils, etc." */
  get utensils(): ApexItem {
    return new ApexItem(this.page, 'P7_UTENSILS');
  }

  /** "+" */
  async clickAdd(): Promise<void> {
    await buttonByLabel(this.page, '+').click();
  }

  /** "Add to Cart" */
  async clickAddCart(): Promise<void> {
    await buttonByLabel(this.page, 'Add to Cart').click();
  }

  /** "Remove from Cart" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Remove from Cart').click();
  }

  /** "Update Cart" */
  async clickEdit(): Promise<void> {
    await buttonByLabel(this.page, 'Update Cart').click();
  }

  /** "Share" */
  async clickShare(): Promise<void> {
    await buttonByLabel(this.page, 'Share').click();
  }

  /** "-" */
  async clickSubstract(): Promise<void> {
    await buttonByLabel(this.page, '-').click();
  }
}
