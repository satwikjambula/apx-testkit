/**
 * GENERATED by apx-testgen from pages/p00001-home.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class HomePage {
  static readonly alias = 'home';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, HomePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** textField, "Item1" */
  get item1(): ApexItem {
    return new ApexItem(this.page, 'P1_ITEM1');
  }

  /** textField, "Item2" */
  get item2(): ApexItem {
    return new ApexItem(this.page, 'P1_ITEM2');
  }

  /** "Button" */
  async clickButton(): Promise<void> {
    await buttonByLabel(this.page, 'Button').click();
  }

  /** "Disable Button" */
  async clickDisableButton(): Promise<void> {
    await buttonByLabel(this.page, 'Disable Button').click();
  }

  /** "Enable Button" */
  async clickEnableButton(): Promise<void> {
    await buttonByLabel(this.page, 'Enable Button').click();
  }
}
