/**
 * GENERATED by apx-testgen from pages/p00012-file.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class FilePage {
  static readonly alias = 'file';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, FilePage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** displayOnly, "Download" */
  get downloadLink(): ApexItem {
    return new ApexItem(this.page, 'P12_DOWNLOAD_LINK');
  }

  /** hidden */
  get filename(): ApexItem {
    return new ApexItem(this.page, 'P12_FILENAME');
  }

  /** fileUpload, "File Blob" */
  get fileBlob(): ApexItem {
    return new ApexItem(this.page, 'P12_FILE_BLOB');
  }

  /** hidden */
  get fileCharset(): ApexItem {
    return new ApexItem(this.page, 'P12_FILE_CHARSET');
  }

  /** textField, "File Comments" */
  get fileComments(): ApexItem {
    return new ApexItem(this.page, 'P12_FILE_COMMENTS');
  }

  /** hidden */
  get fileMimetype(): ApexItem {
    return new ApexItem(this.page, 'P12_FILE_MIMETYPE');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P12_ID');
  }

  /** selectList, "Project", required */
  get projectId(): ApexItem {
    return new ApexItem(this.page, 'P12_PROJECT_ID');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Add File" */
  async clickCreate(): Promise<void> {
    await buttonByLabel(this.page, 'Add File').click();
  }

  /** "Delete" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
