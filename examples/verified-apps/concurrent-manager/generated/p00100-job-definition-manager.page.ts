/**
 * GENERATED by apx-testgen from pages/p00100-job-definition-manager.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class JobDefinitionManagerPage {
  static readonly alias = 'job-definition-manager';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, JobDefinitionManagerPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** switch, "Enabled" */
  get enabled(): ApexItem {
    return new ApexItem(this.page, 'P100_ENABLED');
  }

  /** switch, "Has Calendar" */
  get hasCalendar(): ApexItem {
    return new ApexItem(this.page, 'P100_HAS_CALENDAR');
  }

  /** switch, "Incompatibility" */
  get incompatibility(): ApexItem {
    return new ApexItem(this.page, 'P100_INCOMPATIBILITY');
  }

  /** checkboxGroup, "Job Type" */
  get jobType(): ApexItem {
    return new ApexItem(this.page, 'P100_JOB_TYPE');
  }

  /** "Create New" */
  async clickCreate(): Promise<void> {
    await buttonByLabel(this.page, 'Create New').click();
  }
}
