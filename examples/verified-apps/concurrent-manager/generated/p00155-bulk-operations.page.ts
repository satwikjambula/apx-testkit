/**
 * GENERATED by apx-testgen from pages/p00155-bulk-operations.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class BulkOperationsPage {
  static readonly alias = 'bulk-operations';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, BulkOperationsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Action" */
  get action(): ApexItem {
    return new ApexItem(this.page, 'P155_ACTION');
  }

  /** textarea, "Reason" */
  get reason(): ApexItem {
    return new ApexItem(this.page, 'P155_REASON');
  }

  /** hidden */
  get requestIds(): ApexItem {
    return new ApexItem(this.page, 'P155_REQUEST_IDS');
  }

  /** displayOnly, "Result counter" */
  get resubmitCount(): ApexItem {
    return new ApexItem(this.page, 'P155_RESUBMIT_COUNT');
  }

  /** "Submit Bulk Operation" */
  async clickSubmit(): Promise<void> {
    await buttonByLabel(this.page, 'Submit Bulk Operation').click();
  }
}
