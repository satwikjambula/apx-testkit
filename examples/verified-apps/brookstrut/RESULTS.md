# brookstrut — verification results

Generated by `@apx/testgen` from a real Oracle sample-app export. **The raw
`.apx` export data is NOT included here** — only this project's own derived
tool output (generated Playwright specs + this results summary). See
`.ai/knowledge/verification.md` for why: redistribution terms for Oracle's
actual sample *applications* (as opposed to the documented APEXlang format
itself, which `docs/license-check.md` did clear) have not been resolved.

## Parse

- **Warnings**: 0 (zero-warnings bar met)
- **Pages**: 48
- **Regions**: 134
- **Items**: 101
- **Buttons**: 182
- **Dynamic Actions**: 20

### Region types found
- `breadcrumb`: 41
- `staticContent`: 25
- `interactiveReport`: 16
- `classicReport`: 15
- `dynamicContent`: 12
- `list`: 6
- `facetedSearch`: 5
- `themeTemplateComponent/contentRow`: 4
- `calendar`: 3
- `smartFilters`: 3
- `plSqlDynamicContent`: 2
- `chart`: 1
- `interactiveGrid`: 1

### Unmodeled component types
Real, present in this export, not yet typed at the parser level — preserved
in `raw` bags, nothing lost (ADR-001): `action`, `axis`, `branch`, `column`, `computation`, `facet`, `filter`, `process`, `savedReport`, `series`, `validation`

## Generation

`@apx/testgen` produced **48** page object + smoke spec pairs
(47 marked skip: auth required, gated on
`APX_LOGIN_TEST_USERNAME`/`APX_LOGIN_TEST_PASSWORD`). See `generated/` for
the actual files.

## Determinism

Generated twice from the same export — byte-identical output, both times.
`apx-diff` self-diff (this export against itself):
**0 added, 0 removed, 0 changed, 48 unchanged** — confirms zero spurious differences.

## Live verification

**Live verification**: no running instance available for this app — static ground truth only (parser output, determinism, and structural findings below). No runtime claims are made about this app beyond what a live instance would be needed to confirm.
