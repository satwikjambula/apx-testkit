/**
 * GENERATED by apx-testgen from pages/p00041-load-data.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class LoadDataPage {
  static readonly alias = 'load-data';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, LoadDataPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** hidden */
  get days(): ApexItem {
    return new ApexItem(this.page, 'P41_DAYS');
  }

  /** hidden */
  get loadTime(): ApexItem {
    return new ApexItem(this.page, 'P41_LOAD_TIME');
  }

  /** hidden */
  get orders(): ApexItem {
    return new ApexItem(this.page, 'P41_ORDERS');
  }

  /** hidden */
  get rows(): ApexItem {
    return new ApexItem(this.page, 'P41_ROWS');
  }

  /** hidden */
  get size(): ApexItem {
    return new ApexItem(this.page, 'P41_SIZE');
  }

  /** "Reload Data" */
  async clickReload(): Promise<void> {
    await buttonByLabel(this.page, 'Reload Data').click();
  }

  /** "Up" */
  async clickUp(): Promise<void> {
    await buttonByLabel(this.page, 'Up').click();
  }
}
