/**
 * GENERATED by apx-testgen from pages/p00025-notification.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class NotificationPage {
  static readonly alias = 'notification';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, NotificationPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** datePicker, "Display From" */
  get displayFrom(): ApexItem {
    return new ApexItem(this.page, 'P25_DISPLAY_FROM');
  }

  /** textField, "Display Sequence" */
  get displaySequence(): ApexItem {
    return new ApexItem(this.page, 'P25_DISPLAY_SEQUENCE');
  }

  /** datePicker, "Display Until" */
  get displayUntil(): ApexItem {
    return new ApexItem(this.page, 'P25_DISPLAY_UNTIL');
  }

  /** hidden */
  get id(): ApexItem {
    return new ApexItem(this.page, 'P25_ID');
  }

  /** textarea, "Notification Description" */
  get notificationDescription(): ApexItem {
    return new ApexItem(this.page, 'P25_NOTIFICATION_DESCRIPTION');
  }

  /** textField, "Notification Name", required */
  get notificationName(): ApexItem {
    return new ApexItem(this.page, 'P25_NOTIFICATION_NAME');
  }

  /** selectList, "Notification Type", required */
  get notificationType(): ApexItem {
    return new ApexItem(this.page, 'P25_NOTIFICATION_TYPE');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Create Notification" */
  async clickCreate(): Promise<void> {
    await buttonByLabel(this.page, 'Create Notification').click();
  }

  /** "Delete" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Delete').click();
  }

  /** "Apply Changes" */
  async clickSave(): Promise<void> {
    await buttonByLabel(this.page, 'Apply Changes').click();
  }
}
