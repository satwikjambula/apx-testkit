/**
 * GENERATED by apx-testgen from pages/p00013-delete-multiple-events.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class DeleteMultipleEventsPage {
  static readonly alias = 'delete-multiple-events';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, DeleteMultipleEventsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** selectList, "Calendar" */
  get calendarId(): ApexItem {
    return new ApexItem(this.page, 'P13_CALENDAR_ID');
  }

  /** datePicker, "Until" */
  get endDate(): ApexItem {
    return new ApexItem(this.page, 'P13_END_DATE');
  }

  /** selectList, "Event Type" */
  get eventType(): ApexItem {
    return new ApexItem(this.page, 'P13_EVENT_TYPE');
  }

  /** datePicker, "From" */
  get startDate(): ApexItem {
    return new ApexItem(this.page, 'P13_START_DATE');
  }

  /** selectList, "Timeframe" */
  get timeframe(): ApexItem {
    return new ApexItem(this.page, 'P13_TIMEFRAME');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Delete Events Listed Below" */
  async clickDelete(): Promise<void> {
    await buttonByLabel(this.page, 'Delete Events Listed Below').click();
  }

  /** "Clear" */
  async clickP13Clear(): Promise<void> {
    await buttonByLabel(this.page, 'Clear').click();
  }

  /** "Go" */
  async clickP13Go(): Promise<void> {
    await buttonByLabel(this.page, 'Go').click();
  }
}
