/**
 * GENERATED by apx-testgen from pages/p00005-filter-options.apx — DO NOT EDIT.
 * Typed accessor page object built on @apx/testkit primitives — no raw
 * selectors. Item accessors rest on the VERIFIED apex.item contract; button
 * methods use accessible-role/label locators (region/button DOM convention
 * still open — see docs/grammar-assumptions.md).
 */
import type { Page } from '@playwright/test';
import { ApexItem, apexPageUrl, buttonByLabel, gotoApexPage } from '@apx/testkit';
import { APP_BASE } from '../playwright.config.js';

export class FilterOptionsPage {
  static readonly alias = 'filter-options';

  constructor(private readonly page: Page) {}

  url(): string {
    return apexPageUrl(APP_BASE, FilterOptionsPage.alias);
  }

  /** Navigate here and arm the console guard; returns any console/page errors seen. */
  async goto(): Promise<string[]> {
    return gotoApexPage(this.page, this.url());
  }

  /** checkboxGroup, "Calendars" */
  get calendars(): ApexItem {
    return new ApexItem(this.page, 'P5_CALENDARS');
  }

  /** checkboxGroup, "Calendars" */
  get calAll(): ApexItem {
    return new ApexItem(this.page, 'P5_CAL_ALL');
  }

  /** selectList, "Contact Email" */
  get contactEmail(): ApexItem {
    return new ApexItem(this.page, 'P5_CONTACT_EMAIL');
  }

  /** checkboxGroup, "Event Types" */
  get etAll(): ApexItem {
    return new ApexItem(this.page, 'P5_ET_ALL');
  }

  /** checkboxGroup, "Event Types" */
  get eventTypes(): ApexItem {
    return new ApexItem(this.page, 'P5_EVENT_TYPES');
  }

  /** "Cancel" */
  async clickCancel(): Promise<void> {
    await buttonByLabel(this.page, 'Cancel').click();
  }

  /** "Reset All" */
  async clickReset(): Promise<void> {
    await buttonByLabel(this.page, 'Reset All').click();
  }

  /** "Set" */
  async clickSubmit(): Promise<void> {
    await buttonByLabel(this.page, 'Set').click();
  }
}
