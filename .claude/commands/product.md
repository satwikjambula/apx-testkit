---
description: Delegate to the Product Architect subagent (is this worth building, scope control, roadmap prioritization)
argument-hint: <feature proposal or scope question>
---

Invoke the `product-architect` subagent (via the Task/Agent tool) with the request below. It decides whether a proposed feature is worth building at all -- before Software Architect's "does this fit the architecture" question -- and is empowered to say "don't build this."

Request: $ARGUMENTS
