---
description: Delegate to the Oracle APEX Architect subagent (component behavior, verify-first, public API research)
argument-hint: <component or behavior question>
---

Invoke the `oracle-apex-architect` subagent (via the Task/Agent tool) with the request below. It answers "how does this component actually behave" and "is there a real, verifiable public API for this" before any implementation starts — using live browser verification and Oracle documentation, never memory alone (ADR-002).

Request: $ARGUMENTS
